[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "always"
healthcheckPath = "/health"
watchPatterns = ["services/intelligence/**"]
