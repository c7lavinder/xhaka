# Session Notes — Feb 27, 2025

## Gunner Backend — Command Center Dashboard
- Rebuilt audit page as split-pane Command Center (Design 5, Corey's pick)
- Sidebar agent list + detail panel + triggers tab + EXACT OUTPUTS section
- Commits: `179ef3e`, `24203f9`, `092a860`, `6c5761c`
- File: `gunner-backend/docs/dashboard/audit.html`

## OAuth Token Persistence (Railway)
- Tokens stored in `OAUTH_TOKENS` Railway env var via GraphQL API — survives deploys
- Auto-refresh every 30min
- Commits: `238222f`, `0f2dd86`
- Manual webhook register: POST `/setup/oauth/register-webhooks` (`e8d8318`)
- OAuth connected, webhooks registered for new backend

## Twilio SMS Compliance
- **Lead gen account** (AC0618a75...): Purple Doors brand, trustedflipper.com, campaign CEOXF38. Response sent Feb 27 (72hr deadline).
- **Acquisitions account**: New Again Houses, newagainhouses.com. Response drafted (~Mar 4 deadline, 5 biz days).
- All SMS templates must include STOP language going forward.
- Single brand "Purple Doors" for lead gen cold texting.

## POA/Conservatorship Deal
- Daughter has unnotarized POA for mother with dementia
- Attorney Derek: Note + Quitclaim Deed for $1,500 tax payment, then conservatorship
- Tim Hill attorney for conservatorship process
- Contract expires 4/11. Kyle reaching out to daughter.

## Sub-Agent Config
- Anthropic models fail on sub-agent spawn ("Unknown model" error)
- Workaround: use `gemini-pro` for all spawns
- Claude Code integration not yet wired into OpenClaw
- Sub-agents max concurrent: 8

## Completed Late Session
- **Email bot** wired to real GHL API (`POST /conversations/messages`) — was a stub (`62e3088`)
- **TC Packager** now sends email to title company (reads `playbook.underContract.tcEmail`)
- **Dispo Packager** now sends email to dispo manager (reads team member with role `dispo_manager`)
- **Pipeline auto-discovery**: `GET /setup/pipelines` — pulls all GHL pipelines + stages for onboarding
- **Default model switched** from gemini-pro → `anthropic/claude-sonnet-4-6` (heartbeat too) to fix Google 429 rate limits
- **nah.json** already had all 6 pipelines wired (Sales, Dispo, Buyer, Follow Up, JV Deals, Lead Mining)
- `craftKylePrepTask`/`BUCKET_TO_GHL` renames already done — not in codebase
- Coaching flags already wired and functional in classifier.ts
- GHL→CRM string cleanup: checked — only env var refs remain (GHL_LOCATION_ID etc), no user-facing strings to rename

## Still Pending
- **TC email address** needed — add `tcEmail` to `nah.json` under `underContract` (asked Corey)
- Sub-agents STILL can't use Anthropic models — "Unknown model" error persists. gemini-pro works but unreliable (231ms no-op). Need to investigate OpenClaw sub-agent model routing.
- Latest commit: `62e3088` on main
