# Memory — February 20, 2026

## Key Decision: Lead Gen Manager Deferred

**Decision:** Hold off entirely on Lead Gen Manager for now. Build V2 with data that already comes in (GHL pipeline, existing calls, conversations). When Lead Gen Manager gets built out, that's when we address anything touching lead acquisition.

**Impact:** No changes to current build order (Config → Audit → Rulebook → Bots → GHL Integration). None of those depend on Lead Gen Manager anyway.

## New Lead Process — Major Architecture Day

### Core Principles Locked (ARCHITECTURE.md)
- **Intelligence Services understand. Bots execute.** — hard line, applies everywhere
- **All agents industry-agnostic. Playbook dictates output.** — swap Playbook = new industry, zero code changes

### Step 2 — Data Hygiene (LOCKED)
- 11 bots: 10 simple + Property Condition Bot (Street View grading, reports back to agent)
- BatchLeads for property data (RentCast dropped)
- Lead Intel Bot removed — deferred to Lead Gen Manager
- 4 flags: Timezone Bot (geocoding API), Market Identifier (needs Config), Mailing/Property Data Bot (verify BatchLeads fields), Property Condition Bot (Street View API + vision model)
- One hygiene note per contact — includes condition grade

### Step 3 — Lead IQ (LOCKED)
- Renamed from "Lead Scoring" — owned by Lead IQ Sub-Agent
- 5 scoring factors: Timeline, Motivation, Price (intel services) + Source, Condition (bots)
- 3+ factors = HOT, fewer = WARM, no COLD
- Assignment Bot reads assignee from Config (not hardcoded) — NAH = Daniel
- Note Bot writes: score breakdown (which factors passed/failed + why) + actions taken
- SLA owned by Watchdog, not Lead IQ
- 5 flags: sparse input on intel services, Price Analyzer baseline undefined, Condition Scorer depends on flagged bot, Assignment Bot needs Config, returning lead history needs DB

### Step 4 — Initial Outreach (LOCKED)
- Pure first message sender — Playbook drives industry voice, zero hardcoded industry terms
- Context Bot reads existing contact data (no new fetches)
- Config Reader Bot pulls field names from Config
- **Outbound Manager** = centralized send layer for ALL outbound across every agent
- Message Crafter + Tone Matcher kept separate (Tone Matcher reusable across all SMS agents)
- 3 returning lead tiers: <7 days / 7-90 days / >90 days
- Send window: 8am–11pm (overnight leads queued, message acknowledges gap honestly)
- 5 time-of-day message tones based on send time vs submission time delta
- Message ID written back to contact (for reply tracking)
- **Response Analyzer** + **Message Performance Service** flagged — what raises the ceiling over time
- DNC not applicable — these leads opted in by submitting
- Email Bot flagged for later (99% come in with phone)

### Outbound Manager (New System Component)
- Centralized service all agents submit messages to
- Owns: queue, send window enforcement, dedup across agents, Message ID assignment, logging
- No agent has its own send queue

### Working file: /Users/wholesaleai/gunner-v2/public/org.html

---

## Afternoon Session (Feb 20) — Continued Process Map Work

### Playbook Messages (Initial Outreach — high floor)
8 message scenarios defined for wholesale:
- Instant, Morning, Afternoon, Evening, Overnight→8am, Returning <7d, Returning 7-90d, Returning >90d
- **Company name rule**: form/PPL = include company name, sms/dialer = HIDE company name (protect identity until qualified)
- Rule is Playbook per source — applies to ALL messages including returning lead variations
- Never hardcoded

### Industry-Agnostic Rule (ARCHITECTURE.md — locked)
"Agents are pluggable. The Playbook dictates output."
- No agent, bot, or intel service hardcodes industry terms
- Test: could it run for a racquet sports facility if Playbook swapped? If no → flag
- All industry terms in code = flag → move to Playbook

### Step 5 — LM Calls the Lead
- Escalation contact: Config: escalationContact (not "Jessica" hardcoded)
- 15-min SLA, escalate at 30 min — enforced by Watchdog
- Call Analyzer fires automatically on call end (not a human decision)
- Decision node renamed: "Call Analyzer — What happened on the call?"

### Step 6a — LM Runs the Conversation (UPDATED)
- Script from Playbook — never hardcoded
- LM has Lead IQ note + context before dialing (score, extracted data, missing fields)
- Call records automatically
- Call Analyzer fires the moment the call ends — LM does nothing after conversation
- Handoff: Call Analyzer fires → LM Assistant Sub-Agent

### Step 7a — Post-Call Automation (UPDATED, needs decomposition)
**Two intelligence services:**
- **Call Analyzer** — 6 outputs: call type, conversation summary, extracted data, prospect sentiment, LM performance signals, recommended next step. Playbook-driven, fully industry-agnostic.
- **Gunner Analyzer** — fires INDEPENDENTLY (not coupled to CRM workflow). Scores call quality, script adherence, coaching feedback → feeds Gunner XP/leaderboard/coaching. Playbook-driven (current Gunner code is hardcoded to wholesale — V2 refactors this).

**Key rules:**
- Real conversation → complete Lead IQ task (check it off) before creating next task
- Assignment Bot: if stage change requires new owner (e.g. apt booked → AM), reads from Config
- Confidence threshold: if unsure, flags for LM confirmation

**⚠ DECOMPOSITION FLAG:**
- Step 7a does two distinct jobs: (1) understand the call, (2) execute on it
- Gunner Analyzer fires independently — it's a coaching concern, not a pipeline concern
- Full breakdown deferred to LM Assistant Agent design session

### Step 6b — New Lead Drip Agent (REBUILT)
- **New agent: New Lead Drip Agent** (not Responder — different job)
- Pluggable across industries — wording from Playbook
- Same SMS stack as Step 4: Message Crafter, Tone Matcher, Context Bot, Config Reader Bot, Note Bot, Outbound Manager
- **No Task Bot** — Lead IQ task is the ONLY task in the system for this lead
- **No Tag Bot** — no tags for drip sequence state
- Response Analyzer flagged (reply trigger TBD)
- Company name Playbook rule applies (same as Step 4)
- Sequence duration from Playbook (NAH: 104 days)
- **Turns off + checks off Lead IQ task immediately on real conversation**

### Step 6c — LM Manual Call Attempts (NEW)
- Human step, runs **days 1–12 only**, concurrent with 6b drip
- LM calls daily — Watchdog monitors, flags if no attempt logged
- Stops on: contact made OR day 12 (Ghosted Agent fires)
- 6b drip continues after day 12 — only calls stop

### Step 8 — Ghosted Agent (NEW COMPONENT)
- Fires at **day 12** — time-based, NOT attempt-based
- Day count comes from Playbook (NAH: 12 days)
- Stops 6c (manual calls), drip in 6b continues its full duration
- Does NOT fire if contact already made
- Outputs: status:ghosted tag, note on contact
- Contact made at any point after ghosting → back to Step 5

---

## Late Afternoon Session (Feb 20) — Architecture Finalization

### Shared Services Registry (7 Layers)
All agents call services from the registry — agents are orchestrators only, not skill holders.
Improving a service improves every agent that calls it automatically.

1. **Lead Intelligence** — scoring, price analysis, response classification
2. **Call Intelligence** — Call Analyzer, Gunner Analyzer
3. **SMS Intelligence** — Message Crafter, Tone Matcher, Response Analyzer, Message Performance Service
4. **CRM Operations** — Tag Bot, Note Bot, Field Writer Bot, Pipeline Bot, Assignment Bot, Task Bot, Context Bot, Config Reader Bot, Activity Logger, Status Check Bot, History Check Bot, Dedup Check Bot
5. **Contact Processing** — phone normalization, email validation, name parsing
6. **Data Enrichment** — Timezone Bot, Market Identifier, Mailing Address Bot, Business Name Bot, Property Data Bot, Property Condition Bot (all callable by ANY agent, defined by Playbook per industry)
7. **Guard Pattern** — idempotency + eligibility check; standard opening move for every AI agent

### Key Architecture Rules (LOCKED)
- **Guard Pattern** = standard opening move for every AI agent (Steps 1, 2, 3, 4, 6b, 7a, 8)
- **Context Bot** added to Steps 1, 2, 3, 7a
- **Config Reader Bot** added to Steps 1, 2, 3, 7a, 8
- **Field Writer Bot** writes structured field values (NOT prose, NOT tags) — added to Steps 2, 7a
- **History Check Bot** = Gunner DB lookup for returning leads → sets flag in contact_context table
- **Dedup Check Bot** = CRM dedup check → added to Steps 1 and 2
- **Activity Logger** = logs all call/SMS/email attempts with metadata; any agent can query — added to Steps 6b, 7a
- **Sequence State** = DB-backed drip day tracker per contact, decoupled from CRM — New Lead Drip reads/writes it
- **Notifier** = internal team alerts only (distinct from Outbound Manager); Config-driven channel (GHL task/SMS/Slack)

### Step 8 Final Details
- Status Check Bot aborts if Lead IQ task already complete (dedup guard)
- Outputs: Tag Bot (status:ghosted) + Pipeline Bot (Ghosted stage) + Task Bot (checks off Lead IQ task)
- No note needed (covered by existing drip notes)

### Trigger Logic (LOCKED)
- 6b/6c trigger: assigned user's first double-dial AFTER Lead IQ moves contact to Warm/Hot stage
- "First double-dial of current cycle" only — Watchdog guards against re-triggering
- Answered 1st or 2nd call → Step 6a
- Voicemail detected → 6b does NOT start; LM follows up manually; ambiguous detection flagged
- Callback/text → Response Analyzer + LM inbox
- Response Analyzer trigger: CRM adapter emits standardized `inbound_message` event; GHL = workflow webhook; core never touches transport

### Confidence Threshold (Playbook-configurable)
- ≥85% = auto-execute
- 60–84% = execute + flag for LM review
- <60% = LM confirms before any CRM action

### NAH Playbook Created
**Path:** `/Users/wholesaleai/gunner-v2/playbooks/nah/PLAYBOOK.md`
**Contains:** HOT/WARM task templates, drip schedule (Day 0→104), voice/tone rules, call outcome routing, assignee routing, business rules, enrichment bot sequence, "real conversation" definition, confidence thresholds, hard stop criteria, idempotency windows, tenant config reference

### Intelligence Loop → Playbook Decision (LOCKED)
- Intelligence does NOT bypass Playbook to feed agents directly
- Intelligence improvements → update the Playbook → all agents + future tenants inherit automatically
- New tenants inherit CURRENT best version of Playbook at onboarding (not frozen Day 1 baseline)
- Versioning strategy for Playbook updates: still needs to be formally defined

### "Not a Fit" Routing
→ Follow Up Supervisor (12-month bucket)

### Process Map Status: LOCKED
- All steps 1–8 complete and deployed live at `https://gunner-v2-production.up.railway.app/org`
- Working file: `/Users/wholesaleai/gunner-v2/public/org.html`
- Ready to begin building foundations/infrastructure

### Build Order (Foundation First)
1. Config System ← **NEXT — nothing else starts without this**
2. Audit Log
3. Rulebook
4. Bot Layer (7 bots, atomic, reliable)
5. GHL Integration Layer

### Open Flags Before Build
- BatchLeads API: verify Zestimate availability, mailing address fields, equity estimate
- Google Street View API key not configured → blocks Property Condition Bot
- Price Analyzer: Zestimate availability from BatchLeads unverified
- Step 7a: still needs full decomposition (deferred to LM Assistant design session)
- Follow Up Supervisor: short-term nurture + 12-month bucket — not yet designed
- Acquisitions Supervisor: receives leads when appointment booked — not yet designed
- Playbook versioning strategy: how intelligence-driven improvements apply — not yet defined
- Call Script: referenced by Call Analyzer + Gunner Analyzer, content TBD

---

## Evening Session (Feb 20) — Foundations Complete + First Agent Live

### All 5 Foundations Built
1. **Config System** — TenantConfig, ConfigReaderBot, singleton init
2. **Audit Log + DB** — Postgres on Railway, 3 tables (audit_log, sequence_state, contact_context), Guard Pattern idempotency
3. **Rulebook** — 6 composable rules, Guard class (idempotency + policy in one call)
4. **Bot Layer** — 10 bots: NoteBot, TagBot, PipelineBot, TaskBot, SmsBot, FieldWriterBot, AssignmentBot, ContextBot + 2 stubs
5. **GHL Integration** — GHLAdapter + GHLClient (all CRM ops in one place)

### Webhook Entry Point LIVE
- `POST /webhooks/ghl` — GHL sends events here
- Responds 200 immediately, processes async (GHL retries on non-200)
- Normalizes GHL payload → GunnerEvent (CRM-agnostic)
- Routes to New Lead Supervisor on new-lead events

### New Lead Supervisor LIVE
- Orchestrates Step 2 → Step 3 → Step 4 in sequence
- Aborts on error, logs failure, skips don't block pipeline

### Data Hygiene Agent LIVE (Step 2)
- Guard Pattern (5-min idempotency window + feature flag)
- Phone normalization (E.164)
- Email validation
- Property data enrichment via BatchLeads API
- Market identification
- Property Condition Bot: STUB (needs Street View API key)
- FieldWriterBot writes enriched values to GHL contact
- NoteBot writes formatted hygiene note to GHL
- Full audit entry with actions performed

### Infrastructure
- Postgres DB added to Railway project (auto-deploys on startup)
- DATABASE_URL wired via Railway reference var
- BATCHLEADS_API_KEY injected into Railway env

### What's Next
1. **Lead IQ Agent** (Step 3) — score, assign, move to Warm/Hot, create task, write note
2. **Initial Outreach Agent** (Step 4) — first SMS, Playbook-driven message
3. **GHL Workflow setup** — configure GHL webhook to point at /webhooks/ghl

### GHL Webhook URL (for NAH setup)
`https://gunner-v2-production.up.railway.app/webhooks/ghl`

---

## Late Evening Session (Feb 20) — Steps 3 & 4 Live + Autodiscovery Built

### All 3 New Lead Process Steps Now Live
- **Step 2 (Data Hygiene)** — live, confirmed deployed
- **Step 3 (Lead IQ Agent)** — live
  - Guard Pattern idempotency (24h window)
  - ContextBot loads existing contact data
  - 5 parallel intelligence services: Timeline, Motivation, Price Gap, Source Classifier, Condition Scorer
  - `computeScore()` → HOT (3+ factors) or WARM
  - PipelineBot moves to Warm/Hot stage (creates opportunity if none exists — handles GHL new lead flow)
  - AssignmentBot assigns from Config (NAH = Daniel)
  - TaskBot: ONE task per lead cycle (checked by `hasOpenTask()`)
  - NoteBot writes score breakdown (which factors hit/missed + why) + actions taken
  - TagBot applies hot/warm tag
  - FieldWriterBot writes score fields
- **Step 4 (Initial Outreach Agent)** — live
  - Guard Pattern (24h idempotency — no double-send)
  - Hard stop: no phone = abort
  - ContextBot + Config reader
  - Tone Matcher selects scenario based on time delta
  - Message Crafter (Gemini-powered, 8 scenarios, company name visibility rule)
  - Outbound Manager (send window enforcement, dedup, message ID write-back)
  - NoteBot writes what was sent + tone
  - Full audit entry

### Intelligence Services Built
- `src/intelligence/lead/timeline.ts` — AI + rule-based hybrid (clear patterns rule-based, ambiguous → Gemini)
- `src/intelligence/lead/motivation.ts` — same hybrid approach
- `src/intelligence/lead/price-gap.ts` — Price Analyzer (Zestimate if available, else gap-based)
- `src/intelligence/lead/scorer.ts` — `computeScore()` + `buildLeadIQNote()`
- `src/intelligence/communication/message-crafter.ts` — Gemini-powered, Playbook-driven, 8 scenarios

### AI Client
- Provider-agnostic: `gemini` (default), `anthropic`, `openai`
- `parseAIJson()` — graceful fallback on bad JSON
- `AI_PROVIDER=gemini`, `GEMINI_API_KEY` set in Railway
- Model: `gemini-1.5-flash` (free tier, covers NAH volume)

### Outbound Manager Built (`src/core/outbound-manager.ts`)
- Centralized send layer — no agent calls SmsBot directly
- Enforces 8am–11pm send window
- Dedup check before every send
- Message ID write-back to GHL contact

### GHL Webhook Auto-Registration
- `ensureWebhookRegistered()` runs on every startup — idempotent
- Subscribes to: OpportunityCreate, OpportunityStageUpdate, InboundMessage, ContactCreate
- Failure is non-fatal (server still starts)
- 10-minute onboarding goal: tenant provides API key, Gunner handles the rest

### Fallback Poller
- Runs every 10 minutes as safety net
- 24h idempotency (never re-processes)
- Max 20/cycle, 500ms between — won't hammer GHL
- Never crashes server

### Autodiscovery System Built
- `src/setup/ghl-discovery.ts` — queries any GHL account; auto-maps pipelines, stages, custom fields, users to logical names
- `src/setup/env-generator.ts` — takes discovery output → generates ready-to-use env vars; marks unmapped as `NEEDS_REVIEW`
- Same flow for every future tenant onboarding

### NAH GHL Credentials (Extracted)
- **API Key (v1):** `8bf0b94d-cb81-495a-916b-3b1797912d21`
- **Location ID:** `hmD7eWGQJE7EVFpJxj4q`
- **User ID:** `9yWN7uWYEQyAh6KcC53U`
- **Company ID:** `H7s1oGGooRv0k1PGiMad`

### CURRENT BLOCKER: GHL v2 API Required
- GHL v1 REST API (`rest.gohighlevel.com/v1`) returning **401 on `/users/`**
- GHL deprecating v1 — NAH account requires **GHL v2 API** (`services.leadconnectorhq.com`)
- Discovery script needs update to v2 endpoints with correct auth token format
- **Next action:** Update `src/setup/ghl-discovery.ts` to use v2 API, re-run against NAH

### What's Blocking a Real Lead Test
1. GHL discovery script needs v2 API fix → re-run → get pipeline/stage/user IDs
2. Inject those IDs into Railway env vars
3. Then first real lead can flow through all 3 steps

### Next Build Order (after unblocked)
1. Fix GHL discovery for v2 API → run against NAH → wire Railway env vars
2. Test first real lead end-to-end
3. New Lead Drip Agent (Step 6b) — DB-backed sequence, same SMS stack
4. Ghosted Agent (Step 8) — day-12 timer, stops calls
5. Watchdog — SLA monitor, double-dial detector
6. LM Assistant Agent (Step 7a) — needs full decomposition first
