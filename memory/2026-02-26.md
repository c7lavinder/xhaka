# Memory — February 26, 2026 (early morning, post-compaction flush)

## Gunner V2 — Session Work (continuing from Feb 25 late night)

### Trigger Architecture — LOCKED ✅
- `event-bus.ts`, `crm-sync.ts`, `triggers.ts`, `agent-registry.ts` all live
- One poller per data source; agents don't know their triggers; triggers live in playbook
- OAuth token active: `tenantId=default`, `locationId=hmD7eWGQJE7EVFpJxj4q` — `success=106, hits=0, queue=0`
- PIT token daily limit exhausted — OAuth is primary, PIT abandoned
- Rate limiter (`api-throttle.ts`): 5 req/s, burst 10, max backoff 60s, reset endpoint: `POST /api/audit/reset-throttle`

### Known Bug — `newLeads: 0` (UNRESOLVED at session end)
- GHL shows 6 leads in "New Lead" stage but `fetchOppsInStage()` returns 0
- Suspects: wrong stageId in config, pagination across ~148 opps not working right
- Confirmed stageId from MEMORY.md: `newLead → f919c1a7-17da-456f-b8f9-10c1aca62691` (CORRECTED 2/25)
- But summary says the old stageId in context was `a977dd60-4ef9-40e1-9d8a-b62aaa6bb88f` — possible mismatch still in code
- **6 stuck leads**: Connie Martin, Daryel Champion, Charles Cheatham, James Chappell, Kent A Armstrong, Elizabeth Atkins
- Need to inspect `fetchOppsInStage()` and verify `config.stageId('salesProcess', 'newLead')` returns `f919c1a7...`

### Audit Page (design-5.html) — Dual-View ✅
- File: `/tmp/gunner-v2/public/design-5.html`
- Two tabs: 👤 Leads / 🤖 Agents; `#leads-layout` and `#agents-layout` shown/hidden via `setMode()`
- `AGENT_EXPLAINER` object at line 295 — 17 agents with `what`, `when`, `actions` fields
- Explainer card rendered at ~line 559

### In Progress at Session End — `learns` field
- Goal: Add `learns` string to each of 17 agents in `AGENT_EXPLAINER`
- Add "🧠 How it gets smarter" section to explainer card render
- Was mid-task when compaction fired — NOT yet committed to repo

### Latest Confirmed Commits (from summary)
- `37e584d` — latest on `main`
- Auto-deploys to Railway on push

### Repo & Infra
- Repo: `https://github.com/c7lavinder/gunner-v2.git`, branch `main`
- Local: `/tmp/gunner-v2/`
- Prod: `https://gunner-v2-production.up.railway.app`
- Railway service: `38646fdb-45aa-4c50-a742-ffb4540d2268`
- Railway auth: `107983f5-06cc-40b3-92d6-833004dee064`

### Key GHL IDs (confirmed)
- Location: `hmD7eWGQJE7EVFpJxj4q`
- Sales Process pipeline: `tOqQbembKlIoPiXbepP3`
- New Lead stageId (CORRECTED): `f919c1a7-17da-456f-b8f9-10c1aca62691`
- Warm: `34b88324-9bb1-4110-8531-d4271c6c1567`
- Hot: `84c5583f-7b6b-494a-83b2-df9e54db3c8c`

### State Flags (DO NOT change without Corey explicit)
- `DRY_RUN=false` — LIVE
- `NEW_LEAD_DRIP_ENABLED=false` — New Lead Drip OFF

### Next Session Priorities
1. Finish `learns` field in AGENT_EXPLAINER + update explainer card render → commit
2. Debug `fetchOppsInStage()` — verify stageId config, fix pagination → unblock 6 stuck leads
3. Force-process stuck leads: `POST /api/audit/pipeline-trigger?all=true`
4. LM Assistant build (`src/supervisors/lm-assistant.ts`)
5. Task dedup guard in `src/bots/task.ts`
