# 2026-02-25 — Session Notes (early AM, ~11pm–1:19am CST)

## Gunner V2 — Session Summary

### Features Built Tonight

**AM Assistant — walkthrough-no-offer path** (commit `2e0178a`)
- Added `'walkthrough-no-offer'` to `AMDisposition` type and `AM_DISPOSITIONS` array in `call-intel.ts`
- Added case in `am-assistant.ts`: creates 24h task for AM to run numbers and return with offer
- Logs seller signals + call summary in task body
- AM Assistant is now **FULLY BUILT** (was PARTIAL before)

**Pipeline Force-Run Endpoint + Force Pipeline button** (commit `912b8f5`)
- `forcePipelineRun()` and `triggerPipelineCycle()` exported from `src/core/poller.ts`
- POST `/api/audit/pipeline-trigger` — single lead (`contactId`+`opportunityId` in body) or `?all=true` for bulk scan
- ⚡ Force Pipeline button added to auditor toolbar
- Per-lead ⚡ button on each card (event.stopPropagation to avoid card toggle conflict)
- Both bypass idempotency — safe for manual test runs

**LM + AM Role Views** (commit `fe883fb`)
- `/lm` → `public/lm.html` — Lead Manager view
- `/am` → `public/am.html` — Acquisition Manager view
- Routes added to `server.ts`
- Both show: preview mode banner, stats bar, plain-English agent actions, lead cards
- LM: new leads scored/texted, drip status, needs attention
- AM: call outcomes, offers pending, UC monitoring, needs attention
- 🧪 PREVIEW MODE banner → flips to 🟢 LIVE when DRY_RUN=false

**Agent Guide Tab in Auditor** (commit `7c2e527`)
- New "🤖 Agent Guide" tab in main auditor
- 22 agents across 7 phases in `AGENT_DIRECTORY` JS array
- Each card: emoji, name, code ID, plain-English "what it does", trigger, interval, status pill, last-run time
- CSS: `.agent-card`, `.agent-phase`, `.agent-grid`, `.agent-status-pill` classes
- Renders on `switchTab('agents')` call (wired into existing `switchTab` function)
- Status pills: Live (green), Polling (blue), Standby (gray), Preview/Dry-run (orange)

**Complete STEP_HUMAN for all agents** (same commit)
- Previously only 6 agents had humanized step descriptions
- Now covers ALL agents: data-hygiene, lead-iq, initial-outreach, working-drip, lm-assistant, call-coaching, response-agent, follow-up, follow-up-re-engaged, am-assistant, apt-prep, offer-chase, offer-reply, contract-bot, tc-packager, dispo-packager, uc-monitor, post-close, callback-capture, ghosted, already-sold, bucket-re-eval, stage-change
- Disposition-aware descriptions (e.g. offer-made-accepted shows "Offer accepted — moving to UC")

**Improved leadHeadline** (same commit)
- Now covers all LM dispositions: appointment-set, not-right-now, already-sold, wrong-number, no-answer-voicemail, conversation-no-apt
- Covers all AM dispositions: offer-made-accepted, offer-made-pending, offer-rejected, appointment-no-show, walkthrough-no-offer
- Drip-only leads: "⏰ In follow-up sequence — Gunner texting automatically"

**"Unknown Lead" fix** (commit `cce1e06`)
- Root cause: working-drip entries don't log contactName in metadata
- Fix 1: Pipeline API now does batch CRM lookup (`crm.getContact()`) for contacts missing names — up to 20 at a time, parallel, silent on failure
- Fix 2: `working-drip.ts` now logs `contactName` (firstName + lastName from context.fields) in audit metadata going forward
- `getCRM` imported into `audit-router.ts`

**LM view button + STEP_HUMAN links** (same commit)
- `leadHeadline` now handles drip-only leads properly

**5 Design Mockups** (commit `dbfe3b4`)
- `public/design-1.html` — Activity Feed (chronological, phase-grouped)
- `public/design-2.html` — Agent Status Board (table, one row per agent)
- `public/design-3.html` — Kanban (agent per column, cards below)
- `public/design-4.html` — Mission Control (big stats + 3-col agent grid)
- `public/design-5.html` — Split Pane (sidebar agent list + detail panel)
- Sent all 5 screenshots to Corey via Telegram for review
- Corey choosing which design to build into auditor — PENDING decision

### Auditor — Current State
- `/audit` — main system view (developer-facing, full pipeline detail)
- `/lm` — Lead Manager view (plain English, new leads + drip)
- `/am` — Acquisition Manager view (plain English, calls + offers + UC)
- `/audit#agents` — Agent Guide tab (22 agents, descriptions, status)
- All 5 design mockups live at `/design-1.html` through `/design-5.html`

### DRY_RUN Status
- `DRY_RUN = true` confirmed on Railway (verified via GraphQL API)
- Working drip shows `dryRun: false` in audit metadata — this is a display artifact (send window skips before dry-run check), NOT a real send. Messages are NOT going out.
- Outbound manager returns `{ queued: true }` when outside send window (9am-6pm) regardless of dry run state — causes the artifact

### Deployment Chain
- All commits pushed to `https://github.com/c7lavinder/gunner-v2.git`
- Auto-deploys to `https://gunner-v2-production.up.railway.app`
- Latest commits tonight: `2e0178a` → `912b8f5` → `fe883fb` → `7c2e527` → `cce1e06` → `dbfe3b4`

### Corey's Priority Context (from conversation)
- Feels overwhelmed by the scope — reassured that it's already deployed, just needs DRY_RUN=false
- Main concern: not wanting to deal with fires after going live
- Agreed: go live with NEW LEAD flow only first (one dial at a time)
- Before go-live: review auditor dry-run output for 3-4 days
- Validation step: click into 5-10 leads in auditor, confirm scoring and texts look right
- Role views (LM + AM) are critical for team buy-in — team won't trust what they can't see

### Still Pending (carry forward)
- Corey to choose design concept (1-5) for auditor rebuild
- Dispo Pipeline ID from GHL (Corey action) — needed for `nah.json` dispoPipeline config
- TC/Dispo send medium — waiting on Corey decision (SMS vs email to TC)
- Disable GHL automation workflows before go-live: `y0r0nS6fKHeypYaVAAZd` (walkthrough) + `JFnptaNnUpgcHT6ptdDI` (offer call)
- Flip `DRY_RUN=false` — Corey explicit only
- Validate call-intel on 5-10 real GHL call transcripts
- Role views are Phase 1 completion criteria (LM view DONE, AM view DONE) ✓

### Key Technical Notes
- `getCRM()` imported from `'../bots'` (not from integrations directly)
- `forcePipelineRun()` in poller.ts bypasses idempotency (24h window)
- `triggerPipelineCycle()` in poller.ts respects idempotency
- `AGENT_DIRECTORY` in audit.html drives the Agent Guide tab rendering
- `renderAgentGuide()` is called from `switchTab()` — no separate event listener needed

---

## Pending Code Cleanup (still to do — carry forward)

### GHL String Cleanup
- `src/intelligence/call/call-intel.ts` lines ~115, ~143 — possible "GHL note" in AI prompt template strings → replace with "CRM note"
- `src/intelligence/call/follow-up-router.ts` line ~78 — possible "GHL" ref inside AI prompt → replace
- `src/intelligence/apt/apt-content.ts` lines 209, 220, 227 — "GHL" in user-visible task text → config-driven CRM label

### Rename Coupling
- `craftKylePrepTask` → `craftAMPrepTask` | `KylePrepInput` → `AMPrepInput` | `escalateToKyle` → `escalateToAM`
  - Files: `src/agents/apt-prep-agent.ts:34,167`, `src/agents/offer-chase.ts:139,207`, `src/intelligence/apt/apt-content.ts:34,138,192`
- `BUCKET_TO_GHL` → `BUCKET_TO_STAGE`
  - Files: `src/agents/bucket-re-eval.ts:50,82`, `src/intelligence/call/follow-up-router.ts:32,168`

### Design Review
- Design 5 screenshot not captured yet (session ended before that)
- Corey reviewing designs 1–5 — awaiting pick before rebuilding auditor

### Backburner
- `resetFollowUpState` export + wire into `response.ts`
- `/playbook` page — renders `nah.md` as formatted HTML
- Extract `auditTranscript()` to `sold-verifier.ts`
- TC Packager + Dispo Packager send medium (SMS/email to TC) — awaiting Corey decision
- Dispo Pipeline GHL ID + first stage ID — Corey to provide for `nah.json`
- Disable GHL automation workflows before go-live: `y0r0nS6fKHeypYaVAAZd` (walkthrough) + `JFnptaNnUpgcHT6ptdDI` (offer call)
- Coaching flags not yet injected into `call-coaching.ts` prompt body (wired, but not in prompt text)
- `contactName` logging missing from: lead-iq, lm-assistant, response-agent (only data-hygiene + working-drip log it reliably)

## Session End State (1:31 AM Feb 25)
- All major Phase 1 agents built and committed
- Design mockups live and sent to Corey
- DRY_RUN=true — nothing live
- Next human action: Corey picks a design, then we do 3-4 days of dry-run review before go-live

## 5:00 PM — Rate Limit Crisis + Webhook Missing
- Accountability agent was making ~90 GHL API calls every 30 min → caused 429 storm
- Set ACCOUNTABILITY_POLL_INTERVAL_MS=86400000 (24h) to stop the flood
- Poller had a bug: was using `pipeline_stage_id` (invalid GHL param) → always returned 0 leads
- Fixed poller to fetch all opps and filter client-side by stageId
- GHL webhooks API returns 404 for PIT tokens — can't auto-register
- Result: Elizabeth Atkins + Lawrence Gray stuck in New Lead, never processed
- Asked Corey to manually register webhook in GHL Settings → Integrations → Webhooks
  URL: https://gunner-v2-production.up.railway.app/webhooks/ghl
  Events: OpportunityCreate, OpportunityStageUpdate, InboundMessage
- Rate limit should clear in ~10 min, poller fix will catch stuck leads then
