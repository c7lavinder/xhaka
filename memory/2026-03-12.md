# Memory — 2026-03-12 (1 AM session)

## Control Room — Old vs New

### Old vanilla HTML control room (xhaka-production.up.railway.app/control-room)
- **Status:** Abandoned as primary dashboard — too fragile, too hard to maintain
- Root cause of empty sections: JS rendering order issues + Railway build queue backlog (10+ simultaneous builds)
- Tool Intelligence section was the only reliably working section
- Commits `b21751a` (static HTML baked in) + others — deploy never fully resolved before pivot

### New Next.js Control Room (ACTIVE)
- **Repo:** `c7lavinder/openclaw-control-room`
- **Live URL:** `https://xhaka-control-room-production.up.railway.app`
- **Railway Service ID:** `629682d3-c8d4-4907-9845-304587be36b2`
- **Stack:** Next.js 15 (App Router) + TypeScript + Tailwind v4 + Framer Motion + lucide-react
- **Data source:** GitHub API reading from `c7lavinder/xhaka` repo (not local filesystem)
- **Auth:** `GITHUB_TOKEN` + `GITHUB_REPO=c7lavinder/xhaka` set as Railway env vars
- **Pages:** Home, Memory, Agents, Projects, System (Tools + enrichment in progress)
- **API routes:** /api/memory, /api/agents, /api/projects, /api/daily-logs, /api/health
- **Local copy:** `~/projects/openclaw-control-room/`
- **Architect enrichment in progress** (subagent `b74721d8`) — adding Decisions tab, People tab, Tools page, richer Projects, full agent detail

## Railway Canvas — Xhaka Project
- **xhaka-links repo created:** `c7lavinder/xhaka-links` → `https://links-and-docs-production.up.railway.app`
- **Service group "Xhaka Platform"** created manually by Corey in Railway UI (UI-only feature, not API-accessible)
- **Links and Docs service** (`0498adcb`) — empty service, variables = canonical URLs. Railway renders variables as plain text (not clickable). Workaround: deployed tiny links page.
- **Railway Service Groups** confirmed UI-only — no public GraphQL API support

## Key Decisions Made
- Pivot away from vanilla HTML control room → Next.js 15 architecture
- API routes fetch from GitHub (`c7lavinder/xhaka`) instead of local filesystem — enables Railway deployment
- MEMORY.md intentionally kept lean (< 150 lines); richness lives in subfolders
- Scribe writes daily session context to `memory/daily/YYYY-MM-DD.md` at midnight

## Xhaka Intelligence System — Hardening Status
All 10 hardening fixes shipped and Auditor PASSED (commits `e9da70b` through `27b94f4`):
- State persistence, startup validation, heartbeat, synthesize cron fix, catch-up on boot
- OpenAI silent failure handling, log rotation, daily-log writer, registry corruption guard, dep pinning
- nixpacks.toml: forces `npm install --omit=dev` (no lockfile)

## Operator
- Monitors job health + Railway deployment status every 60s
- 3-attempt remediation ladder, immediate escalation for `missing_env`/`code_bug`
- State persists across Railway restarts via `data/remediation-state.json`
- Auditor PASS confirmed ✅

## Open Items (as of 1 AM)
- Architect enriching new control room (in progress)
- Corey to verify new control room once Architect commit lands
- Old `xhaka` control room (vanilla HTML) — not a priority, may deprecate
- `agents/operator.md` still missing input/output contracts
- LangSmith not wired in Gunner `server/_core/llm.ts`
- No end-to-end proof cron jobs fire in production yet
- GitHub fine-grained token should be set to "no expiry"
