# 2026-03-05 — Gunner UI Audit Checklist

## Corey's Feedback (9:55 AM)
- [ ] **Calls Page:** Entries showing only one name and no address (Example: "Donna").
- [ ] **Training Page:** Content not exclusive to role (LG/LM/AM). Switching tabs changes content but doesn't filter strictly enough.
- [ ] **Day Hub:** LM KPI targets not scaling for 2 LMs (should be 2x).
- [ ] **Inbox:** Shows "No recent messages" instead of loading history.
- [ ] **Appointments:** Missing quick action buttons (needs parity with Inbox).

## Investigation Plan
1. **Calls Page:** Check `gunner-v2` or `gunner-engine` call processing logic. Why is address missing?
2. **Training Page:** Check frontend code (if available) or config for role-based filtering.
3. **Day Hub:** Check KPI target logic in `reporting` engine.
4. **Inbox:** Check message history fetching (GHL integration).
5. **Appointments:** Check UI code for action buttons.

## Investigation Progress
- **Bug #1 (Calls/Address):** Found in `gunner-v2/public/am.html` → `renderCallCard` uses `amStep?.metadata?.address` which defaults to empty string. Root cause: address not being populated during call processing (likely in `am-assistant.ts` or call summary engine).
- **Bugs #2-5:** Already documented as known unfixed issues in `gunner-backend/docs/gunner-unfixed-bugs.md` and `gunner-audit-fixes.md`.
- **Key files:** `gunner-v2/public/am.html` (AM view with Calls + Appointments), `gunner-v2/public/lm.html` (LM view), `gunner-v2/public/hub.html` (launcher). No dedicated Training page HTML found in `gunner-v2/public/` — may be served from getgunner.ai app.
- **Control Room:** `gunner-control-room/index.html` is engine status dashboard only, not the UI with bugs.

## Status
- **Acquisition Machine:** On hold pending Entity decision.
- **Gunner Repair:** Priority 1. Investigation started, not yet fixed.
