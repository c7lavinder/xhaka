
## Backburner Items
- **GHL Appointment Workflow Replication** — Need browser access to read NAH Walkthrough Apt Confirmation, NAH Offer Call Apt Confirmation, and Pending Apt workflows in GHL. Currently the appointment-set path in lm-assistant.ts sends confirmation SMS + moves stage but doesn't replicate GHL automation steps. Pick this up when browser relay is available.
- **TC Packager + Dispo Packager — send medium** — Content is built (GHL note). Next step: wire direct send to TC (SMS/email) and direct send to Esteban. Both pull from tenant playbook config. One `sendOutbound()` call each in `tc-packager.ts` and `dispo-packager.ts`. Hold until Corey ready.

---

## Session — Feb 24, 2026 (1:40 AM CST)

### Major Changes This Session

**Process map / business logic audit with Corey (walked through every screen):**

1. **Drip trigger** — first text now fires within 5 min of LM's double dial (not a timer). `lmDialTrigger: true` on bump step. `activateNewLeadDrip` sets `next_send_at = NULL`. `triggerDripAfterLMDial()` releases it.

2. **Send window** — 8am-11pm → **9am-6pm in lead's timezone**. Updated in loader.ts defaults + all pages.

3. **No-answer/voicemail** — removed double-dial alert and callback task creation. Existing overdue task is the pressure. Just triggers drip.

4. **Appointment set** — drip **cancelled** (not paused). No AM task. GHL automation handles prep. Lead IQ task checked off.

5. **Real conversation** — Lead IQ task checked off. **Follow-up text**: "Great talking to you [name]! I'll put it on my calendar to follow back up. Save my number — [LM]". Drip paused.

6. **Day 14 ghosted** — extracted to own `src/agents/ghosted-agent.ts`. Applies ghosted tag + checks off Lead IQ task.

7. **Not right now** (was "not interested") — **non-linear bucket routing** based on transcript signals. 1mo = selling in 30 days but blocked. 4mo = selling in 6 months. 12mo = anyone else (default). Renamed in all UI.

8. **Already Sold** — new `src/agents/already-sold-agent.ts`:
   - Audits transcript: did LM ask "UC or fully sold?"
   - If UC → re-engage, move back to Warm
   - If confirmed sold → auto-move to Lost
   - County records verification task with search URL
   - Coaching flag if LM didn't ask the right question

9. **Wrong number** — GHL opportunity deleted via OpportunityBot.

10. **Coaching** — call summary to GHL only. Scoring/feedback/factors stay in Gunner app.

11. **Data hygiene** — reads incoming GHL contact notes (PPL source notes) via ContactNotesBot.

12. **Follow-up sequence exhausted** — no "follow-up-exhausted" tag.

13. **Returning lead detection** — detects re-entries by querying audit_log for prior runs. Sets `is_returning = true`. Message crafter uses re-engagement tone.

### New Bots (total now 28: 13 action, 15 observation)
- **ContactNotesBot** (observation) — reads GHL contact notes. Closes data-hygiene CRM backdoor.
- **InboundMessageBot** (observation) — reads inbound messages since timestamp. Closes working-drip CRM backdoor.
- **OpportunityBot** (action) — deletes opportunities. Dry-run aware, audit logged.

### Architecture Violations Fixed
- `already-sold-agent.ts`: `auditTranscript()` AI call is inline (pending extraction to `src/intelligence/call/sold-verifier.ts`)
- `data-hygiene.ts`: now uses ContactNotesBot (no more direct CRM)
- `working-drip.ts`: now uses InboundMessageBot, removed ConversationThreadBot direct construction
- `lm-assistant.ts`: uses OpportunityBot for wrong-number deletion

### Key Business Rules Confirmed
- No task created on no-answer/voicemail — existing task accumulates overdue
- Follow-up buckets are direct placements, not linear progression
- "Already sold" requires county records verification; UC = re-engage
- Appointment set → drip ended permanently
- 9am-6pm is the send window (lead's timezone)
- Coaching stays in Gunner app — GHL only gets call summary

### Latest Commits
- `6c493fb` — 4 business logic updates (ghosted agent, already sold agent, follow-up text, not right now rename)
- `0e47290` — process map final sync
- `571a318` — 3 new bots, CRM backdoors closed
- `df71628` — org.html bot list updated

### Still Pending
- Extract `auditTranscript()` from `already-sold-agent.ts` → `src/intelligence/call/sold-verifier.ts`
- GHL appointment workflow replication (needs browser relay)
- CountyRecordsBot (verify sold via Tennessee county assessor APIs — research needed)
- Wire `InboundMessage` webhook → ResponseAgent (currently logged but unhandled)
- Test full call flow end-to-end on a real completed call

---

## Session — Feb 24, 2026 (afternoon/evening CST)

### Major Changes This Session

#### 1. Unified Call Intelligence Layer (`call-intel.ts`)
- Built `src/intelligence/call/call-intel.ts` — **single AI call per transcript** replacing 3 separate classifiers
- `analyzeCall()` returns `CallIntelResult`: disposition, confidence, followUpBucket, bucketReason, motivationScore, sellerSignals[], objections[], appointmentDetails?, coachingFlags[], callSummary, source, callMode
- `followUpBucket` returned for BOTH `not-right-now` AND `conversation-no-apt` — both get AI bucket routing
- Supersedes `CallDispositionBot`, `walkthrough-disposition.ts`, and `follow-up-router.ts` (kept as fallback)
- All prompts use `loadCallContext()` from `knowledge-loader.ts` (NAH playbook + industry knowledge)

#### 2. Knowledge System
- `config/knowledge/industry.md` — universal wholesale RE call intelligence (motivated seller signals, disposition rules, objection meanings, DEAD vs NOT RIGHT NOW, coaching standards)
- `config/playbooks/nah.md` — NAH-specific rules (team, pipeline, scoring, bucket definitions, dead rules, double-dial, PPL, callback, contract, post-close)
- `src/intelligence/call/knowledge-loader.ts` — assembles system prompt, cached, exposes `loadCallContext()`
- **Edit knowledge files = change AI behavior, no deploy needed**

#### 3. New Agents/Supervisors
- `src/supervisors/stage-change.ts` — routes all GHL `stage-changed` events (UC→Contract Bot, Purchased→Post-Close, Lost→cancel all, Ghosted→cancel drip, Appointment→safety net)
- `src/agents/callback-capture.ts` — inbound call handler, mirrors LM Assistant (same 7 disposition paths, same Call Intelligence, same bucket routing)
- `src/agents/contract-bot.ts` — UC trigger, AI confirmation SMS, Kyle task (1h), 24h escalation task
- `src/agents/post-close.ts` — DB-backed 3-touch sequence (24h/48h/7d); `runPostClosePoller()` on 6h cycle; uses `sequence_state` table with `sequence_id = 'post-close:thank-you'`
- `src/core/inbound-call-poller.ts` — polls every 3min for completed inbound calls

#### 4. `coachingFlags` Wiring
- `CallCoachingInput` interface updated to include `coachingFlags?: string[]`
- Flags extracted in single `analyzeCall()` call, passed to `runCallCoaching()` in both supervisors
- Coaching prompt still needs `coachingFlags` injected into the prompt body (NOT YET DONE — pending)

#### 5. Visual Pages Synced
- `public/org.html` — Call Intelligence card (purple, full-width), Knowledge System section (Tab 3), all new agent cards, inbound call poller, non-linear bucket callout, removed all "drip paused" language
- `public/process-map.html` — Phase 6 (closing + callbacks), non-linear bucket description, 6 new agent directory entries, "New Lead Drip paused" → "cancelled + AI bucket placement"

#### 6. Follow-Up Cadence Updated (ALL buckets → 6 touches)
- `1mo`: 7-day intervals, 6 touches (Day 7/14/21/28/35/42), nextBucket → `4mo`
- `4mo`: 20-day intervals, 6 touches (Day 20/40/60/80/100/120), nextBucket → `1yr`
- `1yr`: 60-day intervals, 6 touches (Month 2/4/6/8/10/12), nextBucket → null
- Updated `DEFAULT_CADENCE` in `src/agents/follow-up/organizer.ts`

#### 7. Bucket Re-Evaluation After Seller Response (IN PROGRESS)
- Goal: any non-positive seller reply triggers AI bucket re-evaluation + reassignment
- `upsertState` in organizer.ts resets touch_count/bucket_entered_at — will be exported as `resetFollowUpState`
- Logic to add in `response.ts`: if intent NOT `interested`/`scheduling`, run bucket router on message body, call `resetFollowUpState` if different bucket recommended

### Latest Commits
- `260c0de` — "fix: conversation-no-apt cancels drip + AI bucket placement"
- (cadence change: saved to organizer.ts but not yet committed at session end)

### Still Pending (carry forward)
- Inject `coachingFlags` into the coaching AI prompt body in `call-coaching.ts`
- Export `resetFollowUpState` from `organizer.ts` and wire bucket re-eval into `response.ts`
- Commit cadence + re-eval changes; update `nah.md` playbook with new cadence numbers
- Update visual pages with new cadence numbers
- Build read-only `/playbook` page rendering `nah.md` as formatted HTML
- Extract `auditTranscript()` → `src/intelligence/call/sold-verifier.ts`
- GHL appointment workflow replication (needs browser relay)
- Validate call-intel accuracy on 5–10 real GHL transcripts before go-live
- Flip DRY_RUN=false ONLY when Corey explicitly says "go live"

### Key Architecture Decisions (locked)
- Zero domain knowledge in agents — all rules in `config/playbooks/nah.md` + `config/knowledge/industry.md`
- All SMS via `sendOutbound()` (OutboundManager) — never direct
- All CRM actions via `getBots()` — never direct CRM calls
- `OutboundRequest` has no `scheduledAt` — scheduled sends use DB `sequence_state` table pattern
- Coaching output stays in Gunner app — GHL gets call summary only
- `callback-requested` disposition removed — folded into `conversation-no-apt`
- Dead lead = ONLY confirmed sold, legal threat, or condemned — low motivation NEVER dead

---

## Session — Feb 24, 2026 (evening/night CST)

### Major Changes This Session

#### 1. AM Assistant Overhaul
- **No-show path**: `moveToStage(contactId, 'salesProcess', 'appointment')` (Pending Apt, ID `09016bf4`) + LM reschedule task (24h) + `craftNoShowReEngagement()` AI text
- **Offer-rejected path**: `runBucketReEval()` fires (AI picks follow-up bucket from transcript) + AM task "Follow up with offer" due 1 week; NO Jessica escalation, NO "renegotiate" task
- **Walkthrough-no-offer path** (PENDING): if walkthrough completed without same-day offer → AM task "run numbers and return with offer within 24h" — in playbook rules, not yet wired in am-assistant.ts
- Removed all hardcoded "Kyle" references — agent resolves AM dynamically from config
- `am-call` added as valid `source` in `bucket-re-eval.ts` with 🤝 emoji

#### 2. Appointment Execution Layer
- `src/agents/apt-prep-agent.ts` — fires on appointment-set; resolves `amFirst` from config; passes to intelligence module; sends AI SMS to seller + prep task to AM; cancellation section also uses `amFirst`
- `src/core/apt-poller.ts` — runs every 15min; fires AI-written SMS reminders (pre-appointment, day-before, day-of, etc.)
- `src/intelligence/apt/apt-content.ts` — `amFirst?: string` param on `KylePrepInput` and `CancellationAlertInput`; uses `input.amFirst || 'the AM'`; "Kyle" fully removed from AI prompts
- `src/intelligence/call/appointment-extractor.ts` — "Kyle comes to see it" → "the Acquisition Manager comes to see it"
- `config/nah.example.json` — `pendingAppointment` key removed; uses existing `appointment` key

#### 3. Contract Bot + TC/Dispo Handoff
- `src/agents/contract-bot.ts` — no Jessica escalation; wired TC Packager + Dispo Packager (content built, send medium not yet wired)
- `src/agents/tc-packager.ts` — TC handoff package; "get from AM" not "get from Kyle"
- `src/agents/dispo-packager.ts` — deal package + Dispo Pipeline opportunity; "get from AM" not "get from Kyle"

#### 4. Follow-Up Cadence (FINAL — locked)
- `1mo`: 15-day intervals × 6 (Day 15/30/45/60/75/90 — 90 days total) → graduates to `4mo`
- `4mo`: 60-day intervals × 6 (Day 60/120/180/240/300/360 — 12 months total) → graduates to `1yr`
- `1yr`: 180-day intervals × 6 (Month 6/12/18/24/30/36 — 36 months total) → closes out
- Cadences updated in `organizer.ts` + `nah.md` + both visual pages

#### 5. CRM-Agnostic Architecture Cleanup
- `CRMAdapter` interface confirmed solid at `src/integrations/crm/interface.ts` — no changes needed
- `GHLAdapter` at `src/integrations/adapters/ghl.ts` implements `CRMAdapter` — confirmed
- Removed stale `import { GHLAdapter }` from `post-close.ts` and `offer-chase.ts`
- Fixed `offer-chase.ts` cast: was `getCRM() as InstanceType<typeof GHLAdapter> & {...}` → `getCRM()`
- Fixed no-op cast in `offer-chase.ts`: `(crm as typeof crm)` → `crm`
- Cleaned all user-visible "GHL" strings in `contract-bot.ts`, `lm-assistant.ts`, `am-assistant.ts`, `next-step.ts`
- Cleaned ALL "GHL" from `config/playbooks/nah.md` (0 remaining)
- **PENDING**: `call-intel.ts` lines 115+143 and `follow-up-router.ts` line 78 may have "GHL" inside AI prompt template strings — not yet confirmed/fixed
- `tsc --noEmit` → CLEAN after all changes

#### 6. Visual Pages Fully Synced
- `public/org.html` — LM Assistant `PARTIAL→LIVE`; AM Assistant `LIVE→PARTIAL`; no-show/offer-rejected/apt-prep all updated; "Kyle"/"Esteban" removed; contract task "for AM"; apt prep "AM always prepped"
- `public/process-map.html` — no-answer boxes merged to single "No Answer"; "7 paths"→"6 paths"; cadence timelines updated; "Kyle" removed; no-show/offer-rejected descriptions updated

#### 7. ROADMAP.md Created
- `gunner-agents/ROADMAP.md` and repo root `ROADMAP.md` — full build order locked
- Phase 1: Acquisition → Phase 2: Dispo → Phase 3: Lead Gen → Phase 4: KPIs → Phase 5: Wizard
- V3 adaptive list vision documented (role-based priority list, replaces pipeline UI, built on `needs-attention.ts` + Pipeline Signals)
- **Do NOT build Wizard early** — needs all phases represented first

#### 8. Latest Commits
- `22279ea` — hardcoded names removal (Kyle/Esteban/Jessica removed from agents)
- `4f6526c` — visual pages fully synced with code
- `fc83b98` — apt stage fix (Pending Apt = appointment stage)
- `2144c62` — LM reschedule task on no-show
- `ec70936` — offer-rejected bucket + task (no renegotiate)
- `a2c69b9` — cadence extension (15d/60d/180d final)
- `37d5fb6` — org.html status badges + names
- `d7a3161` — ROADMAP V3 vision
- `7378a87` — KPIs reordered to Phase 4

### Still Pending (carry forward to next session)
- **AM Assistant walkthrough-no-offer path** — if AM completes walkthrough without same-day offer, create task "run numbers and return with offer within 24h"
- **Fix GHL in AI prompt strings** — inspect `call-intel.ts:115,143` and `follow-up-router.ts:78`; replace "GHL note" → "CRM note" if inside template literals
- **Wire TC/Dispo send medium** — `sendOutbound()` in `tc-packager.ts` and `dispo-packager.ts` when Corey ready
- **Dispo Pipeline IDs** — Corey to provide GHL Dispo Pipeline ID + first stage ID for `nah.json`
- **Inject `coachingFlags`** into coaching AI prompt body in `call-coaching.ts`
- **Export `resetFollowUpState`** from `organizer.ts` + wire bucket re-eval into `response.ts`
- **Build `/playbook` page** — renders `nah.md` as formatted HTML
- **Extract `auditTranscript()`** → `src/intelligence/call/sold-verifier.ts`
- **Validate call-intel** on 5–10 real GHL transcripts before go-live
- **Disable GHL automation workflows** for walkthroughs (`y0r0nS6fKHeypYaVAAZd`) and offer calls (`JFnptaNnUpgcHT6ptdDI`) before going live
- **Flip DRY_RUN=false** — ONLY when Corey explicitly says "go live"

---

## Session — Feb 24, 2026 (late night CST)

### Key Business Context Corrections (from Corey)
- **Offer Reply Agent**: Most common reply is a STALL ("still waiting", "not ready yet", "dealing with something first") — not accept/counter/reject. Stall = reset chase clock, not cancel.
- **UC Monitor (original design was wrong)**: TC work happens OUTSIDE GHL in TC's own CRM. The fires Kyle watches for are seller COMMUNICATION issues post-contract:
  - Sellers have rapport with Kyle and keep texting him
  - Real fires: second thoughts, dispute on terms, confusion about unexpected contacts, closing questions
  - NOT: inspection findings (that flows TO seller), NOT: access requests (buyer-side coordination)
- **Process Map insight**: UC Monitor needed to show up IN the process flow after Under Contract, not just in the agent directory

### What Was Built

#### Offer Reply Agent — 5 outcomes (stall added)
- `accept` → cancel chase → AM task 30 min
- `counter` → cancel chase → AM task with extracted dollar amount (1h)
- `stall` (most common) → **reset** chase from today → AM task "call to understand blocker + get decision date" (24h)
- `reject` → cancel chase → Bucket Re-Eval
- `unclear` → cancel chase → AM task to call back (1h)
- `resetOfferChase()` added to offer-chase.ts (resets to touch 0, restarts Day 3/7/14 from today)

#### UC Monitor — rebuilt as seller communication watchdog (3-tier routing)
- Runs every 30 min
- For every unread seller message in UC stage: classify → route
- **auto-reply**: `general` check-ins → AI sends reassuring text from AM's number. No human sees it.
- **tc**: `closing-logistics` (title/signing/payment) OR `confusion` ("someone called about my house") → AI sends "TC will reach out" to seller + TC task (2h)
- **am**: `concern` (cold feet, family pressure, backing out) OR `dispute` ("I thought we agreed on X") → AM task (30 min, high urgency)
- Also: silence detection — seller was active, went quiet for 5+ days → AM check-in reminder
- New: `src/intelligence/uc/uc-message-classifier.ts` — classifies + routes
- New: `src/intelligence/uc/uc-auto-reply.ts` — crafts AI replies for auto-reply + TC handoff
- Added `'tc'` to `TeamRole` union in types.ts

#### Full Audit — Code + Visuals
- `apt-content.ts`: 3× "update in GHL" → "update in the CRM" in AM prep task body
- Renamed: `KylePrepInput` → `AMPrepInput`, `craftKylePrepTask` → `craftAMPrepTask`, `escalateToKyle` → `escalateToAM`
- Renamed: `BUCKET_TO_GHL` → `BUCKET_TO_STAGE`, `GHLBucketStage` → `BucketStage`
- `org.html` + `process-map.html`: ~20 GHL refs in agent descriptions replaced with CRM-agnostic language
- Stall outcome added to org.html Offer Reply section (was missing)
- UC Monitor added to process-map.html as a visual block WITHIN the Closing Automation phase (between Contract Bot and Post-Close)
- All CRM webhook trigger descriptions updated ("reads GHL" → "reads CRM")

### Latest Commits (late night session)
- `fa6ef83` — UC Monitor + Offer Reply redesigned based on real business context
- `8d18552` — UC Monitor 3-tier routing (auto-reply/TC/AM)
- `1f13fd8` — UC Monitor added to process-map after Under Contract
- `6084519` — UC Monitor categories corrected (concern/dispute/closing-logistics/confusion/general)
- `168a1e4` — Full audit: hardcoded names, GHL strings, CRM-coupled identifiers
- `b3fe4ef` — Full visual audit: stall outcome, all GHL refs in descriptions

### Still Pending (carry forward)
- AM walkthrough-no-offer path in `am-assistant.ts` (in playbook, not in code)
- `call-intel.ts` lines 115/143 — confirmed clean ("contact record" not GHL)
- Wire TC/Dispo send medium when Corey ready
- Dispo Pipeline IDs — Corey to provide
- Flip DRY_RUN=false — Corey explicit approval only
- Role views (V2 completion) — each role needs a view to trust the system
- V2 completion criteria: bots built + each team member has a role view
