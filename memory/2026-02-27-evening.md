# Memory — Feb 27 Evening Session

## 🔥 Webhooks WORKING
- GHL Marketplace App webhooks fully firing as of ~9:30 PM CT
- Root cause: webhooks must be configured in GHL Developer Portal (Marketplace App settings), NOT via API
- Corey had to: publish new app version → uninstall old → reinstall via OAuth link
- 29+ events captured including: ContactCreate, OpportunityCreate, OpportunityStageUpdate, NoteCreate, TaskCreate, ContactTagUpdate, OpportunityAssignedToUpdate, ConversationUnreadUpdate
- gunner-v2 still processing leads live (James Davis moved to Warm by V2)
- New backend catching every event in parallel (dry run)

## GHL Marketplace App Setup (for future reference)
- App name: "Gunner" by Gunner AI (Private)
- Version: v1.1.0 LIVE
- Webhook URL: `https://gunner-backend-production.up.railway.app/webhooks/ghl`
- ALL webhook events enabled
- Private app — install via OAuth link, not marketplace search
- OAuth link: `https://gunner-backend-production.up.railway.app/setup/oauth`
- Webhooks are app-level config, NOT per-location API calls

## OAuth Persistence FIXED
- `RAILWAY_TOKEN` was missing on `gunner-backend` service (was only on `gunner-engine`)
- Fixed: set RAILWAY_TOKEN on service `facfb1ea-9c64-4060-a38f-31564a126106`
- OAUTH_TOKENS now persists to Railway env var — confirmed working
- Token expires: 2026-03-01T03:02:06Z

## Railway Services (correct IDs)
- `gunner-engine` (old V2): `b14d0504-8190-419a-80c5-7dd64dfefcc1`
- `gunner-backend` (new): `facfb1ea-9c64-4060-a38f-31564a126106`
- `gunner-v2`: `38646fdb-45aa-4c50-a742-ffb4540d2268`
- `xhaka-brain` (Postgres): `6a11c690-3dca-4ed9-ad43-3d6b6809c2c1`
- `gunner-watchdog`: `5b3c3c35-c582-42c6-bec2-95f900f856b8`

## State Engine Spec Written
- File: `docs/specs/STATE-ENGINE-SPEC.md` (commit `b62e085`)
- 3 trigger types: Event (instant), Time (poller), AI (classifier)
- Postgres tables: events, lead_state, trigger_log
- Config-driven triggers from playbook JSON
- Identified gaps: lead_state too RE-specific, needs generic entity_state with JSONB, no multi-entity relationships, no multi-step workflows, no outbound webhooks, no priority queue, no A/B testing

## Inngest — Top Pick for Execution Engine
- Researcher recommended Inngest over Temporal, Trigger.dev, BullMQ, n8n
- Native `step.waitForEvent()` — solves speed-to-lead, ghosted detection, drip timing
- Native `step.sleep('48h')` — time-based triggers
- Runs on our existing Express backend, no new infra
- Free tier, usage-based pricing
- Replaces: poller, cooldown system, workflow DAG, timer infrastructure
- We still build: Event Store (Postgres), Lead State, Playbook Config, CRM Adapter
- Cuts build timeline from ~3 weeks to ~1.5 weeks
- Corey asked to validate spec covers "everything now AND future" — I identified 10 gaps to address

## Decisions
- TC Packager sends email (tcEmail: corey@newagainhouses.com for now)
- Dispo Packager sends email to Esteban (estebanldv1999@gmail.com)
- Default model switched to anthropic/claude-sonnet-4-6 (was gemini-pro, 429 errors)
- Sub-agents still can't use Anthropic — gemini-pro workaround, unreliable

## Next Steps
1. Revise State Engine spec with Inngest + fix the 10 gaps (generic entity model, multi-step workflows, etc.)
2. Build Phase 1: Event Store + Lead State + Inngest integration
3. Corey wants it CRM-agnostic AND industry-agnostic — spec must reflect that
4. Latest commit: `b62e085`
