# 2026-02-16

## Corey's Data Hygiene Feedback (CRITICAL — shapes all bot logic)
Corey reviewed all 10 Data Hygiene audit cases. Key patterns:
1. **Source = Lead Type** — NEVER change. PPL stays PPL, dialer stays dialer, etc.
2. **Business Name = Street Address** in GHL
3. **Timezone from property location**, not contact
4. **County records lookup** — pull mailing address, owner info from public records
5. **Contact summary note** — structured note on every contact with all known info
6. **Market tagging** — auto-detect market from address (Nashville, Chattanooga, etc.)
7. **Fake name flagging** — flag suspicious names, cross-ref tax records
8. **Phone area code validation** — flag non-legit area codes
9. **Address casing** — properly formatted
Feedback saved: `audits/corey-feedback.json`
Cron job pulling feedback every 4 hours: `3d0639a2-8766-4256-a7f3-d912750f3e78`

## Late Night Session (continued from 2/15)

### Data Hygiene Bot — Built & Deployed
- Full engine: phone validation, email validation, name normalization, address standardization, PPL source tagging, duplicate detection, quality scoring
- Files: `src/engines/data-hygiene/index.js`, `src/config/data-hygiene-config.js`
- Runs every 2 hours, DRY_RUN respected
- Added to Control Room + Demo + Audit dashboard
- Commits: `d1e630f`, `6cedbc0`, `71eaaed`

### Audit Dashboard — Multiple Fixes
- Added ALL bots to audit (was only 3, now 10 tabs)
- Data Hygiene fetching 50 contacts caused timeout → reduced to 10
- Frontend crash: `Cannot read 'severity' of undefined` — null-guarded `agentDecision`
- Data Hygiene crash: `s.toLowerCase is not a function` — non-string coercion added
- **BIG FIX**: All 10 builders were sequential → switched to `Promise.all()` with 20s per-builder timeouts
- Pre-warm also parallelized

### Automation Pathway — Built & Deployed
- `gunner-demo/pathway.html` — visual subway-map style journey from lead gen to closing
- 50 steps across 9 phases
- Each node shows 👤 human or 🤖 bot with status (active/dry-run/standby/not-built)
- ~49% automation coverage currently
- Corey loved it: "There we go, liking this one so far"

### Dispo Pipeline View — Built & Deployed (sub-agent)
- Visual pipeline in Control Room + Demo showing deal packaging → marketing flow
- Weekly schedule bar (Mon/Tue exclusive, Wed public, Thu/Fri inbound)
- 7 Buyer Lead Gen bots with sample deal data
- Esteban added to Demo team bar

### Audit Dashboard — Loading Fix Saga
- Sequential builders → parallel → batched → FINAL FIX: background-only build
- Page NEVER builds in request path now. Returns 202 "building" + frontend auto-retries every 5s
- Pre-warm starts 10s after deploy, builds sequentially in background
- Commit chain: `d1e630f` → `6cedbc0` → `71eaaed` → `59cd654` → `6b1c189` → `642808f` → `cccb7a5` → `9bd1216`

### Live Feed Tab Added
- ⚡ Live Feed is now first tab on audit dashboard
- Shows every bot action in real-time timeline, auto-refreshes every 30s
- Filters by bot, priority. Pulls from existing `/api/alerts` endpoint
- Commit: `9bd1216`

### Feedback System
- GET `/audit/api/feedback` endpoint added for retrieval
- Cron job every 4h pulls feedback to `audits/corey-feedback.json`
- Corey hammered Data Hygiene with 10 detailed reviews — saved locally

### Data Hygiene V2 Building
- Sub-agent building updates based on Corey's feedback (all 10 items above)

### Key Lessons
- Audit page loading is a recurring problem — too many GHL API calls in one request
- NEVER build in the request path. Background-only + cache is the only pattern that works on Railway
- Corey is a visual learner — pathway/timeline views > tables
- His feedback is incredibly detailed and consistent — use it to shape bot logic
