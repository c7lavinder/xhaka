# 2026-02-16 — Session Notes

## Major Milestones
- **Working Drip + Data Hygiene went LIVE** — first engines out of dry-run
- **Per-engine toggle system built** — `ENGINE_<NAME>=active` env vars, future-ready for admin panel DB config
- **Railway API access** — can now manage env vars directly via GraphQL API (token stored in TOOLS.md)
- **Seller backing out training** — uploaded to Gunner as Objection Handling material for AMs
- **Control Room fixed** — now shows DRY RUN / STANDBY / LIVE accurately per engine

## Technical Changes
- `isDryRun(engine)` — checks `ENGINE_<NAME>` env var before global `DRY_RUN`
- `GHLClient.forEngine(name)` — sets engine context on write methods
- Data Hygiene switched from scheduled 2hr scan → event-driven (pipeline poller triggers on New Lead stage only)
- Control Room reads `engineStatuses` from `/api/status` — dry-run engines show orange DRY RUN badge
- Engine API `/api/engines` now includes all engines (was missing data-hygiene, working-drip, signals, etc.)

## Key Decisions
- **Data Hygiene = new leads only** — Corey: "Only when new ones come in to new lead stage." No retroactive database sweeps.
- **No full-database scans** — too expensive on API calls. Event-driven only.
- **Per-engine activation is the product model** — Gunner add-on marketplace lets customers toggle engines on/off. Today: env vars. Future: tenant DB + admin UI toggles.
- **Control Room is V2** — Manus won't see it for a couple weeks. Backend is priority.

## Commits
- `c5cfb1f` — Per-engine dry-run toggles
- `9bc1344` — Revert full-database Data Hygiene scan (too expensive)
- `7c38860` — Data Hygiene event-driven on New Lead stage
- `d669c7d` — Control Room API: per-engine status
- `2f638e9` — Control Room UI: DRY RUN badges for inactive engines

## Railway Env Vars Set
- `ENGINE_WORKING_DRIP=active`
- `ENGINE_DATA_HYGIENE=active`

## Lead IQ — Full GHL Workflow Replacement
- **Lead IQ went LIVE** — `ENGINE_LEAD_IQ=active` set on Railway
- Replaces GHL Warm/Hot Leads workflows completely (Corey confirmed)
- Source-based routing: `LEAD_IQ_SOURCE_ROUTE_A` (sms/text) and `LEAD_IQ_SOURCE_ROUTE_B` (all other) — both set to Daniel (`G1hAG3KNhzMerkEIvMr5`) for now
- **Internal email notification added** — matches GHL workflow exactly:
  - From: Corey Lavinder <corey@newagainhouses.com>
  - To: assigned user's email (fetched via GHL users API, cached)
  - Subject: `New Lead | {company_name}`
  - Body: `{first_name}, New lead has come assigned to you. It is a {source} lead. Please call ASAP. If no answer, enable the working lead SMS drip. Lead Address: {company_name} Lead Name: {contact.name}`
- Task creation added with title, due date (15 min), assignee
- `sendInternalNotification()` added to GHL client — non-fatal on failure
- `resolveUserEmail()` added to name-resolver — fetches from GHL users API, caches
- Corey needs to disable the GHL Warm/Hot Leads workflows to prevent double-firing
- **GHL sender email for notifications:** corey@newagainhouses.com

## Commits (continued)
- `121020e` — Lead IQ: wire up per-engine toggle
- `a463a14` — Lead IQ: source-based routing + task creation
- `ca9ad32` — Lead IQ: internal email notification (mirrors GHL workflow)

## Working Drip — Full Sequence Built (subagent)
- **22 steps, 104-day sequence** with source branching (dialer/sms/forms/general) for Days 0-2, unified after
- **8 email touchpoints** using `corey@newagainhouses.com` as sender
- `sendEmail()` method added to GHL client
- Committed and pushed to origin main
- **Corey says "the content from this workflow is most important"** — wants to verify our engine matches the GHL Working Leads Drip workflow (`20fc8111`) exactly
- **GHL Workflows API doesn't expose step details** — can't pull SMS/email content or timing programmatically
- **GHL workflow editor won't render** in browser (canvas-based)
- **BLOCKED: Need Corey to screenshot the GHL workflow steps** so we can verify message copy and timing match

## GHL Working Leads Drip — Content Extracted via Browser
- **Old GHL URL format works for workflow editor:** `app.gohighlevel.com/location/{locId}/workflow/{wfId}` (v2 URL doesn't render)
- Extracted to: `gunner-agents/working-leads-agent/GHL-WORKFLOW-CONTENT.md`
- **Key findings vs our engine:**
  - GHL emails come from **Kyle Barks (kbarks@newagainhouses.com)**, NOT Corey — need to update
  - SMS tone is much more casual/personal than what we built ("just bumping this", "I assume you've probably changed your mind")
  - Uses `{{user.first_name}}` (assigned user) and `{{contact.first_name}}` / `(( contact.company_name ))` merge fields
  - Day 0.5 for Dialer/SMS/General is just "** just bumping this - thanks {{user.first_name}}" (very short)
  - Day 2 and Day 14 are "breakup" style messages
  - Form branch gets more detailed messages than other branches
  - Wait timing: not uniform daily — varies (6hr, 1d, 2d, 3d, 4d between steps)
- **Pass 1 captured:** Days 0-5, 7, 10, 14 SMS + email subjects/senders
- **Pass 2 running:** Days 6, 8, 9, 11, 12, 13 SMS + email bodies + post-Day 14 steps

## Still Open
- ~~Working Drip email steps (need sender address from Corey)~~ — **DONE** (using corey@newagainhouses.com) — **NEEDS UPDATE: should be Kyle/kbarks@newagainhouses.com per GHL**
- ~~Corey needs to disable GHL Warm/Hot Leads workflows~~ — **DONE** (Corey toggled both to Draft at 4:23 PM)
- **Update Working Drip engine to match GHL content exactly** — templates, sender, timing all need updating
- Dispo Assist + Buyer Lead Gen (specs done)
- ARV Assistant (spec done)
- KPI detection tuning
