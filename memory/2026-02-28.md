# 2026-02-28 Session

## Playbook Prompt Refactor — Spawned Builder
- Corey identified: 7 agents have hardcoded "real estate" system prompts instead of reading from `playbook.ai.prompts`
- Playbook (`nah.json`) already has `ai.prompts` with templatized, tenant-specific prompts — agents just weren't using them
- Wrote spec: `gunner-backend/docs/specs/PLAYBOOK-PROMPT-REFACTOR.md`
- Spawned Builder (gemini-pro, label: `builder-prompt-refactor`) to execute

### The 7 Agents Being Refactored
1. `initial-outreach.ts` — line ~35, key: `initial_outreach`
2. `follow-up-messenger.ts` — line ~17, key: `follow_up`
3. `buyer-response.ts` — lines ~39,43, key: `buyer_response` (new)
4. `deal-blaster.ts` — lines ~43,49, key: `deal_blast` (new)
5. `morning-briefing.ts` — lines ~161,162, key: `morning_briefing` (new)
6. `buyer-matcher.ts` — line ~69, key: `buyer_matcher` (new)
7. `dispo/buyer-qualifier.ts` — line ~68, key: `buyer_qualifier` (new)

### What's Being Created
- `src/agents/prompt-helper.ts` — shared helper: `getSystemPrompt(tenantId, agentKey)` + `resolveTemplate(template, vars)`
- 5 new prompt entries added to `nah.json` → `ai.prompts`
- `ai.systemContext` added to `nah.json`
- Fallback: if no playbook prompt, builds generic from `playbook.industry` + `playbook.company.name`

### Rule: Zero industry words in agent .ts files after refactor
- No "real estate", "wholesale", "wholesaler", "investor" in src/agents/

## Model Notes
- Main session switched to `anthropic/claude-opus-4-6` at 2:43 PM
- Sub-agents still using `gemini-pro` (Anthropic models fail for spawn)

## Prompt Refactor — COMPLETE ✅
- Builder finished successfully (gemini-pro, 5min)
- All 7 agents confirmed clean: `tsc` 0 errors, `grep` 0 hits
- Cleaned up Builder's messy comments in `deal-blaster.ts`

## Full Trigger Wiring — COMPLETE ✅
- Spec: `docs/specs/TRIGGER-WIRING-SPEC.md`
- Gemini Pro Builder completed all 4 parts
- Poller: 4 checks → 11 (drip, follow-up cadence, apt reminders, accountability, offer chase, reality check, intelligence poller)
- CRM triggers: added call_completed_lm and call_completed_am to nah.json
- Webhook→event bus bridge wired in src/api/webhooks.ts
- Config bridge: src/playbook/config.ts now reads from nah.json, falls back to env vars
- Added `system.periodic` to EventKind
- Corey wants ALL triggers working by Monday March 2

## Two Trigger Systems (important architecture note)
- `src/playbook/triggers.ts` = hardcoded EventKind→agent wiring (internal events: lead.scored, lead.new, etc.)
- `src/engine/event-triggers.ts` = reads `playbook.crm.triggers` from nah.json (CRM webhook events)
- Both kept — they serve different purposes, don't conflict

## Claude Code for Building — WORKING
- API key from memory/2026-02-26-evening.md: `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`
- Flags: `--dangerously-skip-permissions --no-session-persistence`
- Must use `-p` (not `--print`) with `pty: true` for output
- `npx` wrapper hangs on long tasks — unreliable for big builds
- Short tasks work fine via pty
- Gemini Pro sub-agents still more reliable for large builds (prompt refactor + trigger wiring both succeeded)

## Existing Engine State
- Webhook receiver: LIVE (GHL marketplace app, 29+ event types)
- Event store + lead_state + trigger_log: Postgres tables exist and working
- Poller: runs 4 checks (speed-to-lead, ghosted, stale-stage, warm-no-call)
- Needs expansion to: drip schedule, follow-up cadence, apt reminders, accountability, offer chase, reality check
- 54 agent files, all registered. Most toggled OFF except intake agents.

## Full Dry Run Test — 44/44 PASSING ✅
- Created `src/dry-run/simulate-all.ts` — comprehensive test covering ALL trigger paths
- Tests: new lead chain (7), stage changes (12), follow-up (6), inbound messages (2), calls (3), dispo pipeline (9), buyer pipeline (4), accountability (1)
- Fixed export name mismatches: `runUCMonitor`, `runTCPackager`, `runAMAssistant` (uppercase)
- 0 failures, 0 skipped, 2.0s runtime

## Deployed to Railway ✅
- Commit: `73a09f7` — "Full trigger wiring + playbook prompt refactor"
- 19 files changed, +1,242 lines
- Railway service: `facfb1ea-9c64-4060-a38f-31564a126106` (gunner-backend)
- Deploy status: BUILDING as of 4:54 PM CST
- OAuth token expires: 2026-03-01T03:02:06Z (still good through tonight, may need re-auth before Monday)

## Next Steps
- Monitor deploy completes successfully
- Verify webhooks still flowing after deploy
- OAuth token expires March 1 — may need Corey to re-auth before Monday
- Monday: Corey flips agent toggles from false → true to go live
