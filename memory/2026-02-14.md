# 2026-02-14 — Daily Log

## Saturday Morning Check-in with Corey

### Engine Overnight Report
- Gunner engine healthy, 18+ hours uptime, 552+ poll cycles
- **0 action-level events** — all activity classified as info priority
- DRY_RUN still ON
- Team mostly idle on Saturday (expected)

### Critical Gap Found: No BatchDialer/BatchLeads Pollers
- Corey asked why there weren't hundreds of calls showing Friday
- **Root cause:** Engine only polls GHL (contacts, conversations, pipelines)
- BatchDialer and BatchLeads API keys are in Railway env vars but **no pollers were built for them**
- The engine is blind to all raw calling/texting volume
- Only sees calls that result in GHL conversation logs
- Need to build: `contact-poller.js` equivalents for BatchDialer + BatchLeads
- BatchDialer API base URL: `app.batchdialer.com` (tried `api.batchdialer.com` — doesn't resolve)
- `/api/v1/calls` returned 404 — need to find correct API docs/endpoints
- Corey wants this fixed — asked "can you build it now?"

### Engine Architecture Reminder
- Poller files: `src/pollers/` (contact-poller, conversation-poller, pipeline-poller, poll-manager, poll-state)
- Engine source: `/Users/wholesaleai/.openclaw/workspace/gunner-engine/src/`
- GitHub: c7lavinder/gunner-engine (auto-deploys to Railway)

### Name Resolution Fix
- Engine was showing raw contact IDs (cZ5ViBw1MYKB5VcNHEDc) instead of names
- Created `src/utils/name-resolver.js` — maps 10 team user IDs + caches contact lookups from GHL API
- Fixed in: ghl.js (write methods), webhooks.js, transcript.js, team-intel.js, kpi-tracker.js, team-digest.js

### Walkthrough → Offer SLA (Tiered Escalation)
- Corey's rule: after walkthrough, underwriting + offer must happen within 24h
- But sometimes complex properties (rural, foundation, septic) take longer
- **Built 3-tier escalation in crm-scanner.js:**
  - 24h → ⚠️ Clock Ticking (nudge)
  - 48h → 🚨 At Risk (seller may be shopping)
  - 72h → 🔴 Deal Walking (reassign to original LM for chase)
- Tier 3 looks up GHL calendar appointments to find who qualified the lead
  - Routes back to Daniel or Chris (whoever set up the walkthrough)
  - Added `getContactAppointments()` to GHL client
- LM_USER_IDS map: Daniel (G1hAG...) and Chris (nJVGO...)

### Team Process Learned
- Kyle does walkthroughs + makes offers — takes over completely at walkthrough
- Daniel/Chris qualify leads + set up walkthroughs
- If seller ghosts Kyle post-walkthrough, Kyle manually Google Messages Daniel/Chris to chase
- That manual handback is what the engine automates at tier 3
- William Thompson example: walkthrough done ~4 days ago, won't pick up for Kyle

### Pipeline Scan Results (Feb 14)
- 11,884 total opportunities (up from 11,868 on Feb 13)
- 8 appointment "no-shows" — actually most are upcoming walkthroughs (Feb 16-18)
- 5 in Walkthrough Apt Scheduled: Micah Hensley, William Thompson, Kathy Pack, Lourie Saad, James Manuele
- Only Thompson is actually past-due (walkthrough happened, no offer)

### Demo vs Real Control Room
- Corey noticed demo looks much better than real
- Reason: demo has curated fake data, real has limited history + DRY_RUN
- Will improve as engine runs through a full work week

### Corey Context
- Still in Costa Rica
- Up ~10 AM CT, worked until ~12:30 PM CT
- Direct as always — "there should have been a couple hundred calls"
- Wants signals that match his actual process, not generic rules
- Key quote: "24 hours is how it has been wording" (on walkthrough-to-offer SLA)
