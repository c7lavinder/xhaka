# 2026-02-17 — Session Notes

## Working Drip SMS Templates Updated
- Removed timing assumptions ("just sent me your info" / "yesterday") — leads may enter CRM hours after engagement
- Day 0 templates now ask "when's a good time tomorrow morning for a quick call?" instead of asking about the property
- More casual tone throughout (commit `6cf5d03`)

## Lead IQ — Stage Move Added
- Lead IQ now moves contacts to Warm Leads (`34b88324`) or Hot Leads (`84c5583f`) stage in Sales Process pipeline (`tOqQbembKlIoPiXbepP3`)
- Was missing — only tagged before, didn't move pipeline stage (commit `cfe558b`)

## New Lead Responder Built
- Fires immediate SMS right after Lead IQ scores + moves stage
- HOT: "do you have a few minutes today for a quick call?"
- WARM: "when's a good time for a quick call?"
- Source-branched (dialer/sms/forms/general), SMS from assigned team member's name
- File: `src/engines/new-lead-responder/index.js`

## Critical Fix: Chained Processing Order
- **Corey corrected me**: Data Hygiene runs BEFORE Lead IQ, not separately
- Moved Lead IQ from contact poller to pipeline poller, chained AFTER Data Hygiene
- Re-fetches contact post-hygiene so Lead IQ scores clean data
- Contact poller is now observation-only — no engine triggers (commit `519b28c`)

## Critical Fix: Contact Poller ≠ Seller Leads
- **Corey corrected me**: Contacts include buyers, partners, etc. — not just sellers
- All seller processing triggers ONLY from pipeline poller watching Sales Process → New Lead stage
- Removed all engine triggers from contact poller (was firing on ALL contacts)

## LEAD-FLOW.md Created
- `gunner-agents/LEAD-FLOW.md` — single source of truth for the lead process
- Corey was frustrated I kept missing steps — this doc prevents that going forward
- **Must reference this before touching any lead flow code**

## Control Room — New Seller Lead Flow Visual Added
- Same style as Dispo Pipeline visual
- Shows full chain: Lead Enters → Data Hygiene → Lead IQ → Hot/Warm split → New Lead Responder → Team calls → outcomes
- Engine chain bar at bottom showing sequential order
- Commit on gunner-control-room repo (`2fb68fc`)

## Call Summary Bot Built (NEW ADD-ON)
- `src/engines/call-summary/index.js` — writes conversation summary notes for real calls only
- Only fires when actual conversation happened (no "called, no answer" noise)
- Uses Gunner transcript if available, falls back to GHL conversation history
- **Context API**: other engines feed context via `addContext(contactId, { source, text })` → shows as "Engine Intel" in note
- Toggle: `ENGINE_CALL_SUMMARY` — in dry-run

## Task Manager Bot Built (NEW ADD-ON)
- `src/engines/task-manager/index.js` — centralized task creation + deduplication
- All engines route through `taskManager.createTask()` instead of `ghl.createTask()` directly
- 2-hour dedup window (same contact + same task type = no duplicate)
- When off, passes through directly to GHL — nothing breaks
- Toggle: `ENGINE_TASK_MANAGER` — in dry-run

## Add-On Marketplace Architecture Confirmed
- **Corey's pricing model**: Each engine = toggleable add-on behind paywall
- Call Summary Bot = $49/mo
- Task Manager Bot = $49/mo
- Call Summary and Task Manager are SEPARATE bots — Corey explicitly wants them independent
- Call Summary only writes notes, Task Manager only handles tasks
- Other agents influence Call Summary via context API but don't write their own notes

## Key Lesson
- **Always check LEAD-FLOW.md before modifying lead processing**
- **Contact poller catches ALL contacts** — never use it for seller-specific logic
- **Pipeline poller + Sales Process + New Lead stage** = the only seller trigger
- Corey gets frustrated when I miss process steps — the LEAD-FLOW doc fixes this

## Full Lead Chain (MEMORIZE THIS)
```
Seller enters Sales Process → New Lead stage
  → 1. Data Hygiene (clean/enrich/verify)
  → 2. Lead IQ (score → tag → assign → move to Warm/Hot stage → task → email)
  → 3. New Lead Responder (immediate SMS)
  → 4. Team member calls
  → 5. No answer → Working Drip (104-day SMS + email sequence)
```

## All Engines Refactored — No Duplicate Notes/Tasks
- Lead IQ, Follow-Up Manager, Working Drip, Drip Analytics, Data Hygiene all refactored
- `addNote()` calls replaced with `addContext()` feeding Call Summary
- `createTask()` calls replaced with Task Manager routing (with dedupTypes)
- Commit `e71bdce`

## Call Summary + Task Manager Flipped ACTIVE
- `ENGINE_CALL_SUMMARY=active` set on Railway
- `ENGINE_TASK_MANAGER=active` set on Railway

## Lead Watchdog Escalation Is Broken
- Currently just logs SLA breach + tries Google Chat notification (which goes nowhere — Space is observe-only)
- Corey asked "how is this escalating?" — it's not, in any useful way
- **NEEDS FIX**: Should create task via Task Manager, notify team member, and/or ping Corey on Telegram
- Waiting on Corey's answer for who should get the escalation nudge

## New Lead Responder — Time-Aware (commit `46a4c05`)
- Morning/Afternoon (9-5 CT): "I'm going to give you a call here shortly" — primes for LM call within 15-30 min
- Evening: "good time tomorrow morning?"
- Night: "good time to chat tomorrow?"
- HOT leads get more direct wording than WARM

## Lead Watchdog Escalation — Logs Only For Now
- Corey said keep it nowhere — when Gunner V2 publishes, escalation will show in the app
- No external notifications, no tasks, no pings

## Message Intelligence Built (NEW ADD-ON)
- `src/engines/message-intelligence/index.js` — passive data collection
- Tracks: sent SMS per template, replies tied back to templates, sentiment classification, time-of-day performance, source performance, negative reactions
- Persists to `data/message-intelligence.json` every 5 min
- Dashboard: `GET /api/message-intelligence`

## Message Optimizer Built (NEW ADD-ON)
- `src/engines/message-optimizer/index.js` — the accountability layer
- Reads Message Intelligence data, flags underperformers (<10% response after 20+ sends)
- Detects negative patterns (3+ DNC triggers on a template)
- Recommends replacements based on top performers
- Time-of-day and source-specific insights
- Runs daily at 6am CST
- Reports saved to `data/optimization-reports/YYYY-MM-DD.json`
- Template blocklist structure ready for V2 auto-disable
- Dashboard: `GET /api/message-optimizer` + `/recommendations`

## Corey's Architecture Principle
- **Data collection and action are ALWAYS separate agents**
- Message Intelligence = data. Message Optimizer = action. 
- Call Summary = notes. Task Manager = tasks.
- Each independently toggleable behind paywall at $49/mo each

## Speed-to-Lead Built & Active
- Tracks gap from New Lead stage entry → first outbound call/SMS
- Per-user, per-source, per-day stats with SLA compliance (15/30/60 min buckets)
- Persists to `data/speed-to-lead.json`
- Dashboard: `GET /api/speed-to-lead` + `/leaderboard`

## CRM Scanner Flipped Active + Updates
- `ENGINE_CRM_SCANNER=active` set on Railway
- **Ghosted Detector**: 12 days threshold, 15+ outbound attempts, 0 replies → moves to Ghosted stage, adds tag, completes Lead IQ task
- **Walkthrough SLA**: Flags deals stuck in "Walkthrough Apt Scheduled" for 24h+ with zero movement
- **Post-walkthrough offer task**: 3h after walkthrough calendar event ENDS → creates task for Kyle (AM) to make offer, due by 5pm CST next business day
- Working Drip no longer handles ghosted moves — Ghosted Detector owns that lifecycle

## Follow-Up Bot Split Into Two
- **Follow-Up Organizer** — pipeline management, stage moves, notes, tasks, keeping CRM clean
- **Follow-Up Messenger** — actual nurture messaging, personalized SMS/email per stage
- Each a separate $49/mo add-on
- Corey wants GHL workflow content extracted first as baseline floor
- Three GHL workflows being extracted: 1 Month Follow Up, Follow Up Automation, Follow Up Organization
- Workflow IDs: `73b74257`, `0927d584`, `9c959627`
- Sub-agent extracting content to `gunner-agents/follow-up-workflows/GHL-FOLLOWUP-CONTENT.md`

## Bots Corey Rejected/Deferred
- Appointment Confirmation Bot — GHL workflow handles it
- Re-engagement/Unassigned Response Bot — not needed near future
- Contract Countdown Bot — not now
- Disposition Auto-Tagger — killed ("I do not like this")
- Workflow Shadow — original purpose done (GHL workflows disabled)
- After-Hours Bot — redundant with New Lead Responder
- Seller Re-engagement — not needed
- Appointment Bot — GHL handles it

## Engine Statuses (9 active!)
- working_drip: ACTIVE ✅
- data_hygiene: ACTIVE ✅
- lead_iq: ACTIVE ✅
- call_summary: ACTIVE ✅
- task_manager: ACTIVE ✅
- message_intel: ACTIVE ✅
- message_optimizer: ACTIVE ✅
- All others: DRY-RUN 🔒
