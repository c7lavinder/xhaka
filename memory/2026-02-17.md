# 2026-02-17

## Gunner Engine — DRY_RUN Investigation

- Corey reported triggers not firing ("They all say active but no actions yet")
- Investigated: engine is healthy (12+ hours uptime, 369+ poll cycles)
- **Root cause: `DRY_RUN=true` globally** — engines detect events and log actions but don't write to GHL
- Active engines: working_drip, data_hygiene, lead_iq, call_summary, task_manager, message_optimizer, message_queue, opportunity_conductor
- Dry-run engines: signals, nurture, follow_up, scheduling, deals, buyer_leadgen, dispo, arv_assistant, appointment_bot
- Working Drip enrolling contacts (Tina Clay, Rickey Riley, etc.) but only logging, not sending SMS
- Deals engine scanning ~30 deals every 15 min (contract signed, package status) but not acting
- No Lead IQ scoring or Data Hygiene actions visible — either no new seller leads or triggers not met
- **Waiting on Corey's approval to flip DRY_RUN=false**
- Dashboard available at: `https://gunner-engine-production.up.railway.app/dashboard/logs`
- API logs at: `/api/logs/recent`

## Useful Endpoints Discovered
- `/api/status` — full engine status + poll stats
- `/api/logs/recent` — recent action/event logs (JSON, now up to 1000 with priority filter)
- `/api/engines` — engine list
- `/dashboard/logs` — HTML activity feed with filters
- `/dashboard/nurture` — nurture dashboard
- `/dashboard/signals` — signals dashboard
- `/audit/` — agent audit page (rewritten to show execution history)
- `/audit/api/data` — audit data grouped by agent (reads from memory, instant)
- `/audit/api/feedback` — GET/POST feedback per agent/contact

## Audit Page Rewrite (Feb 17)
- Removed heavy GHL pipeline re-evaluation (was 4+ min cache build, often timed out)
- Now reads from dry-run action log (instant, up to 1000 entries in memory)
- Shows last 10 actual actions per agent with timestamps
- Old builder functions saved to `audit-builders.js`
- Corey feedback: UI too complicated with tab bar → rebuilding as Control Room-style card grid

## Corey's Data Hygiene Feedback
- What agent would have done is great
- Does NOT need "phone-needs-verification" tag — only market tag
- Missing mailing address info from county data
- Needs to make note

## Bugs Fixed
- `audit.js`: buyer lead gen crash — `toLowerCase()` on non-string field value
- `audit.js`: timeouts bumped from 30s to 90s for GHL builders (before full rewrite)
- `api.js`: log limit raised from 100 to 1000, added priority filter param
- Added no-cache headers to audit page to prevent stale browser JS

## Working Drip Bug (not yet fixed)
- "messages is not iterable" error on multiple contacts (OivdR8QF6eKAh4A6JgbO, 8nAfo8vUmSvTY3odZc51, etc.)
- Shows in Railway logs, separate from audit issue

## Engine Activity Today (while in DRY_RUN)
- Working Drip: 30+ enrollments
- Deals: 684+ actions (contract signed, package status, distribution checks)
- Signals: 1 critical (Seller Withdrew — John Mosier)
- CRM Scanner: 2 offer SLA breaches (William Thompson 186h, Micah Hensley 165h)
- Task Manager: 2 task creates (make offer tasks)
- Data Hygiene: 0 contacts processed — 10 new leads came in but weren't caught (see fix below)

## Data Hygiene Fix — Newly Created Contact Trigger
- Problem: GHL workflows move leads past "New Lead" stage before 2-min poller catches them
- Fix: pipeline-poller.js now also triggers Data Hygiene on contacts created since last poll in Sales Process pipeline (not just current stage = "New Lead")
- Pushed commit fe39e0d — deployed
- 10 leads from earlier today still missed (already "old" to poller) — need backfill
- Same issue likely affects Lead IQ and New Lead Responder — need to verify

## Corey's Audit UI Feedback (collecting, NO changes yet)
1. **Deals Engine**: Should only activate when property moves to Under Contract in Sales Process, then observe matching address in Dispo Pipeline
2. **Working Drip detail**: Needs to show WHY triggered (which call, when), WHAT messages are queued (Day 1 SMS, Day 3 email, content), which branch
3. **General**: Layout is better with card grid, but needs more workflow context — show the full picture of what will happen, not just "enrolled"
4. **Data Hygiene**: Still not picking up new leads (fix deployed but no new leads since)

## Audit Page Iterations Today
1. Original: heavy GHL re-evaluation, 4+ min cache build
2. Rewrite 1: execution history from dry-run log, tab bar
3. Rewrite 2: card grid (Control Room style) replacing tab bar  
4. Rewrite 3: full SaaS design overhaul — clean typography, plain English, subtle design
5. Hidden raw IDs, friendly op names, no-cache headers
6. Removed inactive grouping — all agents in one grid with status dots
7. 4-column comparison back: Agent Decision / Xhaka's Take / What Happened / Your Call
8. Richer Working Drip details (sequence preview, branch, trigger)
9. Fixed trigger display — was showing raw conversation ID instead of "Unanswered outbound call"

## Corey's Full Feedback Summary (Feb 17)
**Engine Logic (NOT YET DONE):**
1. Deals Engine: Only activate on Under Contract in Sales Process → cross-ref Dispo Pipeline for matching address
2. Tags: Lock to approved GHL tags only — Call Summary inventing tags like "Highly Motivated", "Probate", "Squatter Occupied", "Appointment Set" that don't exist in SOPs
3. Data Hygiene: Fix deployed for new lead detection, but 10 leads from today still missed — need backfill
4. Lead IQ / New Lead Responder: Likely same issue as Data Hygiene (not catching leads that pass through New Lead quickly)

**UI (MOSTLY DONE):**
- Layout better with card grid ✅
- Need workflow context — show full picture of what will happen ✅ (4-column view)
- Working Drip needs why/what/which branch detail ✅
- All agents show as active (dry run) not inactive ✅
- Trigger showing raw IDs fixed ✅ → now says "not answering first call attempt as a new lead"
- "What Actually Happened" needs more detail ✅ (4-column)
- Feedback box for Corey's input ✅

## Engine Overrides Removed (Feb 17 ~4:40 PM)
- 11 `ENGINE_*=active` env vars in Railway were overriding global `DRY_RUN=true`
- Removed all: ENGINE_CALL_SUMMARY, ENGINE_CRM_SCANNER, ENGINE_DATA_HYGIENE, ENGINE_LEAD_IQ, ENGINE_MESSAGE_INTEL, ENGINE_MESSAGE_OPTIMIZER, ENGINE_MESSAGE_QUEUE, ENGINE_OPPORTUNITY_CONDUCTOR, ENGINE_SPEED_TO_LEAD, ENGINE_TASK_MANAGER, ENGINE_WORKING_DRIP
- Task Manager WAS creating real tasks for Kyle (make offer after walkthrough) + duplicating
- All engines now in dry run, confirmed ✅
- **Important:** When re-enabling engines, set individual ENGINE_* vars back in Railway, or set DRY_RUN=false globally

## Audit Page Timeout Issue
- `/audit/` route returns static HTML (52KB template), client fetches `/audit/api/data`
- Was returning 502 (15s timeout) during heavy scan cycles blocking Node event loop
- Fixed by capping log buffer at 1000 (was 5000), adding trim on load
- Also intermittent — works fine between scan cycles (~1.8s response)
