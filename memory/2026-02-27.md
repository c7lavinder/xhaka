# 2026-02-27 Early AM Session (continued from 02-26 night)

## Dispo Pipeline Built
- 9 new agents: deal-blaster, buyer-matcher, buyer-response, showing-manager, offer-collector, dispo-closer, jv-router, deal-terminator, dispo-accountability
- 8 new EventKind types added to event-bus.ts
- 11 new triggers added to nah.json (9 dispo + 2 buyer pipeline)
- All registered in register-toggles.ts
- Dashboard updated with Phase 8 (Dispo) + Buyer Management sections
- Esteban Leiva is Dispo Manager (GHL ID: BhVAeJjAfojeX9AJdqbf)
- Total: 41 agents + 9 bots = 50 switches
- Corey's insight: "Dispo is selling inventory — a lot of businesses and industries will also be based around this"

## Complete Bot Layer Expansion (IN PROGRESS)
Corey called out: "How is there only 9 bots? Shouldn't there be hundreds? Everything should be stripped out of agents or impossible to scale"

Problem found: agents were bypassing bot layer in two ways:
1. Direct ghlGet calls from agents (reality-check, follow-up-organizer)
2. Direct Gemini AI calls from agents (initial-outreach, follow-up-messenger, call-coaching, buyer-response, buyer-matcher, deal-blaster)

Builder spawned to fix this. New bots being created:
- contact-search.ts (searchBot) — search contacts by query/stage/date
- task-reader.ts (taskReaderBot) — read tasks by contact, find overdue
- note-reader.ts (noteReaderBot) — read notes by contact
- opportunity.ts (opportunityBot) — CRUD opportunities
- pipeline-reader.ts (pipelineReaderBot) — get contacts in stage
- ai-writer.ts (aiWriterBot) — wraps generateText, toggleable
- ai-classifier.ts (aiClassifierBot) — wraps generateJSON, toggleable
- email.ts (emailBot) — stub for future email

Rule enforced: `grep -rn "from.*integrations/" src/agents/` MUST return zero results after fix.
Architecture: Agents → Bots → Integrations (each layer independently toggleable/swappable)

## Key Architecture Principle (from Corey)
"Everything should be stripped out of agents or impossible to scale"
- Agents = orchestration logic ONLY
- Bots = all external operations (reads, writes, AI, comms)
- Integrations = raw API clients (GHL, Gemini, etc.)
- Each bot independently toggleable
- Swap Gemini for OpenAI = change 1 bot, not 10 agents

## Deployment Status
- gunner-backend LIVE on Railway: https://gunner-backend-production.up.railway.app
- Service ID: facfb1ea-9c64-4060-a38f-31564a126106
- Health: {"status":"ok","dryRun":true}
- DRY_RUN=true confirmed

## Pure Agent Refactor COMPLETE (commit d9d1492)
5 new bots created:
- classifier.ts — ALL classification logic (message, sold type, disposition, outcome, interest, bucket, tone, UC questions, call scoring)
- template.ts — ALL string/note/SMS construction (14 template functions)
- scheduler.ts — ALL time/scheduling logic (send windows, drip steps, reminders, due/stale checks)
- guard.ts — idempotency/dedup checks
- compliance.ts — DNC/regulatory checks

15 agent files refactored. Verification passed:
- 0 integration imports from agents ✅
- 0 classification functions in agents ✅
- 0 hardcoded keyword arrays in agents ✅
- Agents are now PURE orchestration: isEnabled → bot calls → emit → auditLog

Total bot count: 22 bots
Total agent count: 41 agents
Total toggles: 63 independent switches

## Fault Tolerance (IN PROGRESS)
- Bots return safe defaults when disabled ✅ (empty arrays, null, {result:'disabled'})
- BUT most agents have NO try/catch around bot calls
- If a bot throws (network error, API timeout), the agent crashes and stops
- Need to wrap every bot call in try/catch so one failure never kills an agent
- Agents with try/catch: call-coaching, follow-up-messenger, initial-outreach, outbound-manager, reality-check-poller
- Agents WITHOUT: all others (~35+ agents)
- Builder spawned to add .catch() on every bot call in every agent
- Pattern: bot fails → auditLog error → agent continues to next step
- Top-level try/catch on every agent function too
- Corey's directive: "Pull absolutely everything out of them. Also ensure when you turn one off it doesn't affect another"
- Corey also confirmed: "I don't forget" is the prime directive — memory files are for compaction survival, not sleep

## Files to Watch
- Bot count will go from 9 → ~17 after builder finishes
- register-toggles.ts will have all new bots
- src/bots/index.ts barrel needs updating
- All 10+ agents being refactored to use new bots instead of direct imports
